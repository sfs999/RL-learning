# 预训练语言模型

编码器模型（Encoder-Only）:掩码语言建模（Masked Language Modeling）
    遮掩某个词进行推理
    优点：利用双向上下文，语言理解效果好
    缺点：使用[mask]特殊符号，导致预训练-微调差异；假设所有被mask的token相互独立
    分类回归等判别式任务
    任务：
        分类任务->输出分类
            情感分析
            垃圾邮件检测
            自然语言推理
        回归任务->输出分数
            文本相似度评分
            可读性评分
解码器模型（Decoder-Only）：自回归语言建模（Auto-regressive Language Modeling）
    根据前文推理下一个词
    优点：天然适合生成类任务，容易构造大规模的训练数据
    缺点：局限于单向建模，难以同时利用双向上下文信息
        输入即上下文，<mark>直接开始预测下一个词</mark>
    生成式任务
        写文章、写代码、聊天、翻译、续写
编解码模型（Encoder-Decoder）：序列到序列建模（Sequence to Sequence Modeling）
    可输入mask序列输出完整序列
    兼具判别与生成能力
    通读全文（编码），完全理解意思后，再重新组织语言输出一个全新的、结构不同的序列（解码）。它擅长有明确输入输出对应关系的转换任务
    1. Encoder：双向读取整个输入序列，将其压缩成一个富含语义的“中间向量”。
    2. Decoder：基于这个“中间向量”，自回归地生成输出序列。
    <mark>输入和输出通常是连贯的一整段话</mark>
只要数据量足够大、参数足够多，Decoder-Only 模型通过适当的提示词（Prompting），也能很好地完成翻译、摘要等原本属于 Encoder-Decoder 的任务。它变成了“全能选手”

## Transformer 架构及其变体


**Transformer** 本身并非单一的模型架构，而是一个**基础框架（骨架）**。它由 **Encoder（编码器）** 和 **Decoder（解码器）** 两大核心模块组成。根据任务需求的不同，可以灵活组合或单独使用这两个模块，从而衍生出三种主流的模型架构。

- Transformer 的原始形态
    *   **来源**：2017 年谷歌论文《Attention Is All You Need》。
    *   **架构类型**：**Encoder-Decoder（编解码器）**。
    *   **结构细节**：
        *   左侧堆叠 6 层 Encoder。
        *   右侧堆叠 6 层 Decoder。
        *   两者通过“交叉注意力机制（Cross-Attention）”连接。
    *   **设计初衷**：专门为**机器翻译**任务设计（输入源语言序列，输出目标语言序列）。
    *   **特点**：兼具强大的语义理解能力（Encoder 双向）和序列生成能力（Decoder 自回归）。

研究人员发现可以将原始架构拆解，针对不同任务进行优化，形成了以下三类模型：

- A. Encoder-Only 模型（仅编码器）
	*    **构建方式**：移除 Decoder 部分，仅保留并堆叠 Encoder 模块。
    *   **关键修改**：
        *   移除“因果掩码（Causal Mask）”，启用**双向注意力机制**。
            *   模型可以同时看到当前词左右的上下文信息。
    *   **代表模型**：BERT, RoBERTa, ALBERT。
    *   **适用场景**：**判别式任务（Discriminative Tasks）**。
        *   文本分类、情感分析、命名实体识别（NER）、语义相似度计算。
        *   **优势**：利用双向上下文，对整句语义的理解极其精准  

- B. Decoder-Only 模型（仅解码器）
  *   **构建方式**：移除 Encoder 部分，仅保留并堆叠 Decoder 模块。
  *   **关键修改**：
      *   让 Decoder 直接接收输入序列。
      *   强制施加**因果掩码（Causal Mask）**，确保模型在预测时只能看到当前时刻之前的词（单向建模）。
  *   **代表模型**：GPT 系列 (GPT-3/4), Llama, Qwen, Claude。
  *   **适用场景**：**生成式任务（Generative Tasks）**。
      *   开放域对话、创意写作、代码生成、文本续写。
  *   **优势**：天然适合自回归生成，易于利用海量无标注文本数据进行预训练，是目前大语言模型（LLM）的主流架构。

- C. Encoder-Decoder 模型（完整编解码器）
  *   **构建方式**：完整保留 Encoder 和 Decoder 两部分，维持原始的交互结构。
  *   **关键机制**：
      *   Encoder 负责双向编码输入序列，提取深层语义表示。
      *   Decoder 基于 Encoder 的输出，通过交叉注意力机制进行自回归解码。
  *   **代表模型**：T5, BART, Flan-T5, 原始 Transformer。
  *   **适用场景**：**序列到序列转换任务（Sequence-to-Sequence）**。
      *   机器翻译、文本摘要、语法纠错、问答系统（需生成具体答案时）。
  *   **优势**：在输入和输出结构差异较大、或需要严格基于输入内容进行重写的任务中表现优异。

## 模型训练技术

大模型的完整训练流程通常分为三个核心阶段：**预训练 → 指令微调 → 对齐**。每个阶段在数据、模型、目标和训练时间上各有侧重。

- 1. 预训练（Pre-training）
  - **数据**  
    - 原始数据：千亿级文本、图书、网页、代码等
  - **模型**  
    - 基础模型（Base Model）
  - **目标**  
    - 学习领域知识 / 通用知识
  - **训练时间**  
    - 月级别
  > 此阶段是“打基础”，让模型掌握语言规律和世界知识。
- 2. 指令微调（Instruction Fine-tuning, SFT）
  - **数据**  
    - 标注用户指令 + 对应答案（数万条自然语言指令）
  - **模型**  
    - SFT 模型（Supervised Fine-Tuned Model）
  - **目标**  
    - 提升指令遵循能力（能听懂并执行用户指令）
  - **训练时间**  
    - 天级别
  > 此阶段让模型从“知道”变成“会做”，学会响应具体任务。
- 3. 对齐（Alignment）
  - **数据**  
    - 标注对比对（十/百万级）：
      - 用户指令 vs. 不同回复的偏好排序
      - 用于强化学习或奖励建模
  - **模型**  
    - 对齐模型（Aligned Model）
  - **目标**  
    - 生成更安全、更符合人类价值观的指令响应
  - **训练时间**  
    - 天级别
  > 利用强化学习方法进一步微调大语言模型,训练 RM 来对模型结果进行评分然后使用 PPO 对 SFT 模型进行微调
  > 此阶段确保模型输出“有用、无害、诚实”，避免有害或不恰当回答。

# 大模型知识编辑

与传统的“微调”（Fine-tuning）不同，知识编辑的目标不是让模型学习新技能或适应新领域，而是像“外科手术”一样，仅对模型内部的某一项事实进行植入、修正或删除，同时**严格保持模型其他知识和通用能力不受影响**。

大模型在预训练完成后，其参数化的知识通常是静态的，面临以下挑战：
- 知识过时：世界在不断变化（如“现任美国总统是谁”），预训练数据无法覆盖最新信息。
- 事实错误：模型可能记住了错误的知识或产生幻觉。
- 隐私与安全：需要删除模型中不应存在的敏感信息或偏见。
- 微调的局限性：如果用单条数据（例如“李宏毅是全世界最帅的人”）去微调一个大模型，往往会导致灾难性遗忘（忘记旧知识）或过度泛化（无论问什么都回答李宏毅），破坏模型的整体能力。

一个成功的知识编辑方法通常需要满足三个关键原则（参考李宏毅教授2025课程及前沿研究）：
1.  **可靠性（Reliability）**：编辑后，模型必须能准确回答被修改的事实（例如：问“谁是美国总统？”能正确回答更新后的名字）。
2.  **泛化性（Generalization）**：模型不仅要在直接提问时回答正确，还要能在相关的推理任务中体现新知识（例如：问“美国总统的夫人是谁？”也能基于新事实正确推导）。这也被称为“多跳推理”能力。
3.  **局部性（Locality）**：这是最关键的一点。编辑操作不能影响无关的知识。例如，修改了“美国总统”的信息，不应导致模型不知道“地球是圆的”或改变了其编程能力。

## 主要技术方法
目前的研究主要分为两大类路径：

- A. 不改变模型参数的方法（外部/情境编辑）
  *   **原理**：保持模型权重不变，通过在输入端附加额外的信息或检索外部知识库来“欺骗”或引导模型输出正确结果。
  *   **代表技术**：
      *   **IKE (In-Context Knowledge Editing)**：利用上下文学习（In-Context Learning），在提示词（Prompt）中动态插入示例，告诉模型新的事实。
      *   **RAG (检索增强生成)**：虽然常被视为独立架构，但在广义上也可看作一种不改动参数的知识更新方式，通过检索外部最新文档来回答。
  *   **优点**：安全、可逆、不影响原模型。
  *   **缺点**：依赖上下文长度，推理成本可能增加，难以处理复杂的隐性知识更新。

- B. 改变模型参数的方法（内部/参数编辑）
  *   **原理**：直接定位并修改模型神经网络中的特定参数（权重），将新知识“写入”模型内部。这常被比喻为给AI打上“思想钢印”。
  *   **代表技术**：
      *   **ROME (Rank-One Model Editing)**：通过分析模型的注意力机制，定位存储特定事实的关键神经元，利用秩一矩阵更新技术直接修改权重。
      *   **MEMIT / MEND**：ROME的扩展，支持批量编辑多个事实。
      *   **AlphaEdit / ACE**：引入零空间约束（Null-space constraints），确保在更新新知识时，投影到旧知识方向上的参数变化为零，从而更好地保护旧知识（解决局部性问题）。
      *   **CaKE (Circuit-aware Knowledge Editing)**：2025年提出的新方法，关注模型内部的“推理回路”，通过激活完整的推理路径来实现更具泛化性的编辑，解决多跳推理失效的问题。
  *   **优点**：推理速度快（无需额外上下文），知识内化，泛化潜力大。
  *   **缺点**：技术难度高，若控制不好容易破坏模型其他能力（局部性难保证）。

## 技术原理

**大模型确实是黑箱，知识确实是分布式存储的，直接修改权重确实极易破坏统计分布**。早期的研究者也有同样的担忧，但随后的研究发现，虽然大模型整体是分布式的，但在**特定层级**和**特定神经元**上，存在一种令人惊讶的“局部化”现象。

- 1. 如何确定待编辑节点？（定位机制：Locate）

这并非盲目猜测，而是基于对 Transformer 架构内部信息流动的**因果追踪**（Causal Tracing）技术。

**核心发现：知识存储在 MLP 层**
研究（如 ROME 论文）通过大量实验发现，在 Transformer 结构中：
  *   **自注意力层**（Self-Attention）：主要负责信息的传递、复制和上下文关联（类似于“快递员”），它们把上一层的输出搬运到下一层。
  *   **前馈神经网络层**（MLP / Feed-Forward Networks）：才是真正**存储事实性知识**的地方（类似于“数据库”或“键值对存储器”）。

**具体定位步骤（以 ROME 为例）**：
假设我们要修改事实：“埃菲尔铁塔位于巴黎” -> “埃菲尔铁塔位于罗马”。
1.  **输入提示词**：给模型输入 "The Eiffel Tower is located in"。
2.  **追踪最后一词**：模型在预测下一个词（"Paris"）时，我们需要知道是哪个层的哪个神经元起了决定性作用。
3.  **因果介入**（Causal Mediation Analysis）：
    *   研究者会进行“消融实验”：暂时屏蔽某一层某一神经元的输出，看模型预测 "Paris" 的概率是否大幅下降。
    *   如果屏蔽了第 5 层的第 1024 号神经元，模型突然不知道埃菲尔铁塔在哪了，或者概率暴跌，那么这个神经元就被标记为**关键节点**（Key Neuron）。
4.  **锁定目标**：通常，关于“主语 - 属性”这类事实的知识，往往集中存储在模型**中后部**（约 60%-80% 深度）的某些特定 MLP 层中。

**结论**：我们不是随机选节点，而是通过数学手段找到了那些对特定事实贡献度最高的“关键记忆单元”。


- 2. 如何通过修改权重实现知识替换？（修改机制：Edit）

找到节点后，并不是简单地“把代表巴黎的权重改成代表罗马的权重”（因为权重本身没有语义标签，是一堆乱码数字）。

**核心思想：秩一更新**（Rank-One Update）
**原理拆解**：
在 MLP 层中，计算过程可以看作是一个**键值对**（Key-Value）的检索系统：
*   **Key **(K)：识别输入模式（例如检测到“埃菲尔铁塔”这个概念）。
*   **Value **(V)：输出对应的属性（例如输出“位于巴黎”的向量表示）。
*   **公式**：输出 = $W_{out} \cdot \text{ReLU}(W_{in} \cdot \text{Input})$。其中 $W_{in}$ 充当 Key，$W_{out}$ 充当 Value。

**操作步骤**：
1.  **构造新映射**：我们希望当输入是“埃菲尔铁塔”的向量表示（$k_*$）时，输出变成“位于罗马”的向量表示（$v_*$）。
2.  **计算残差**：计算当前模型输出与新目标输出之间的差距（残差）。
3.  **秩一矩阵更新**：
    *   我们不重写整个巨大的权重矩阵 $W$。
    *   我们计算一个极小的修正矩阵 $\Delta W$，它的秩为 1（即它可以分解为两个向量的外积：$\Delta W = u \cdot v^T$）。
    *   **新权重** = **旧权重** + **修正矩阵** ($W' = W + \Delta W$)。
4.  **数学魔法**：这个 $\Delta W$ 的设计非常精妙，它保证：
    *   对于输入 $k_*$（埃菲尔铁塔），输出强制偏移向 $v_*$（罗马）。
    *   对于其他所有输入 $k_{other}$，由于正交性或投影关系，$\Delta W \cdot k_{other} \approx 0$，即**几乎不产生影响**。

**比喻**：
想象一个大图书馆（模型），书架（权重）上摆满了书。
*   **微调**相当于把整个图书馆的书重新整理一遍，风险很大。
*   **知识编辑**（ROME）相当于发现某个书架的第 3 排第 5 格专门放“埃菲尔铁塔”的信息。我们不是把那格书撕了，而是**在那一格上面贴了一张透明的修正胶片**。当你透过胶片看“埃菲尔铁塔”时，看到的是“罗马”；但当你透过胶片看旁边的“卢浮宫”时，因为胶片在那个角度是透明的，看到的依然是原来的内容。


- 3. 为什么不会破坏统计分布？（对抗黑箱与分布式存储的质疑）

答案是：**既是，也不是**。这里有几个关键的反直觉事实：

**“稀疏激活”与“超定位”（Superposition）的解耦**
虽然理论上知识是分布式的，但在大模型的**中间层**，神经元表现出惊人的**模块化**特征。
*   研究发现，对于特定的事实（如“首都”、“出生地”），模型倾向于将其压缩存储在少数几个特定的神经元子空间中。
*   这种现象被称为**“线性可分离性”**。虽然底层参数是纠缠的，但在高维空间中，不同概念的向量方向往往是近似正交的。
*   **秩一更新的优势**：正是利用了这种高维空间的几何特性。我们在一个极小的子空间方向上进行修改，而这个方向与其他知识的存储方向夹角接近 90 度（正交），因此互不干扰。

**局部性约束（Locality Constraints）**
这就是为什么后来出现了 **AlphaEdit** 和 **MEND** 等方法。
*   早期的 ROME 确实会破坏分布（你担心的情况确实会发生，编辑多了模型就傻了）。
*   改进后的方法引入了**零空间**（Null-space）：
    *   在计算 $\Delta W$ 时，强制要求 $\Delta W$ 必须垂直于“保留知识”的特征空间。
    *   数学上保证：对于任何属于旧知识分布的输入 $x$，都有 $\Delta W x = 0$。
    *   这就从数学原理上**物理隔离**了新知识与旧知识，防止了统计分布的崩塌。

**C. 冗余性（Redundancy）**
大模型参数量巨大（千亿级），存在大量的冗余。
*   同一个事实可能在多个地方有微弱的备份。
*   当我们强力修改了最主要的“主记忆节点”后，模型在推理时会优先采信这个最强的信号，从而覆盖掉其他微弱的旧信号。这就像你在嘈杂的房间里大声喊出一个新指令，大家都会听你的，而忽略背景里的低语。

随意修改权重确实会破坏模型。但现代知识编辑技术（特别是 2024-2026 年的进展）之所以有效，是因为：
1.  **精准定位**：利用因果追踪找到了知识的“主存储区”（MLP 层的特定神经元），而不是盲目乱改。
2.  **几何隔离**：利用高维空间的正交性，通过**秩一更新**和**零空间约束**，确保修改只发生在极小的子空间内，不影响其他方向的知识分布。
3.  **最小干预**：只加一个极小的修正矩阵，而不是重写权重，最大程度保留了预训练时的统计规律。

**局限性依然存在**：
尽管效果惊人，但这依然是一种“黑客式”的修补。如果编辑次数过多（例如几万次），或者编辑的内容与原有知识逻辑冲突太剧烈（例如强行让模型认为“1+1=3”且要保持数学体系不崩），目前的数学约束终究会失效，模型依然会崩溃。

# 蒸馏学习

- 监督微调（SFT）
    - 作用：对齐模型输出与人类解题格式，学习标准推理步骤
    - 数据要求：需高质量链式标注
- 知识蒸馏
    - 流程：大模型（教师）生成CoT数据 → 小模型（学生）SFT学习推理模式
    - 代表方案：DeepSeek-R1蒸馏版Qwen-7B性能超越32B基线模型

- 蒸馏三步法：
    - 数据生成：教师模型生成高质量CoT数据
    - 拒绝采样：筛除错误答案，保留逻辑清晰、格式规范的推理链
    - SFT微调：小模型直接学习教师输出，无需额外RL阶段

# 大模型水印

**大模型水印（LLM Watermarking）** 是一种在由大型语言模型（LLM）生成的文本中嵌入不可见、但可被特定算法检测的统计特征的技术。其核心目的是在不影响人类阅读体验（即文本质量、流畅度和语义）的前提下，为AI生成的内容打上“数字指纹”，以便后续能够：
  1.  **溯源与版权保护**：识别文本是否由特定的模型生成，防止模型滥用或侵权。
  2.  **内容鉴别**：区分人类创作内容与机器生成内容，对抗虚假信息传播。
  3.  **责任追踪**：在出现有害内容时，追踪生成该内容的模型来源。

与传统的图像水印不同，文本水印不能简单地叠加像素，因为文本是离散的符号序列。因此，LLM水印通常通过微调模型生成下一个词（Token）的概率分布来实现，使得生成的文本在统计上呈现出某种特定的模式（即水印），只有拥有密钥的检测器才能识别这种模式。

- 三种文本水印算法详解
  - 1. KGW (Kirchenbauer-Goldwasser-Wu) 算法
      是最经典、最早期的基于Logits的水印算法，由Kirchenbauer等人在2023年提出。
      *   **核心原理**：**红绿名单机制（Red/Green List）**。
          *   **划分**：在生成每一个Token时，算法根据前一个Token（或上下文）通过一个伪随机哈希函数，将词汇表动态划分为两个集合：“绿名单”（Green List）和“红名单”（Red List）。
          *   **偏置（Bias）**：在模型采样之前，人为地增加绿名单中Token的Logits值（即增加它们被选中的概率），同时保持或降低红名单Token的概率。
          *   **检测**：检测器使用相同的密钥和哈希规则重建红绿名单，统计生成文本中落在绿名单上的Token比例。如果比例显著高于随机期望（通过Z-test检验），则判定存在水印。
      *   **优点**：实现简单，无需训练模型，直接干预推理过程。
      *   **缺点**：
          *   **鲁棒性差**：对轻微的文本修改（如同义词替换、语序调整）非常敏感。一旦攻击者改变了部分Token，导致上下文哈希变化，红绿名单就会完全改变，水印信号迅速消失。
          *   **质量影响**：强制偏好绿名单可能会略微降低文本的自然度，尤其是在绿名单中没有合适的语义候选词时。
  - 2. SIR (Semantic-Invariant Robust / Steganographic Implicit Representation) 算法
      *SIR通常指代一类尝试结合语义或学习上下文感知偏置的方法，旨在解决KGW的脆弱性。在此语境下，它特指那些利用神经网络学习更优偏置策略的中间阶段算法。*
      *   **核心原理**：**上下文感知的学习型偏置**。
          *   与KGW使用固定的哈希规则不同，SIR类算法引入一个轻量级的神经网络（编码器），根据当前的上下文动态学习如何调整Logits。
          *   它试图找到一种扰动方式，使得水印信号不仅存在于表面的Token选择上，还能在一定程度上抵抗语义不变的变换。
          *   **分离训练**：早期的SIR方法通常将水印编码器（用于嵌入）和检测器（用于提取）分开训练，或者仅优化嵌入部分，检测部分仍依赖统计检验。
      *   **优点**：相比KGW，能更好地适应上下文，提升了在轻微编辑下的鲁棒性。
      *   **缺点**：
          *   **非端到端**：编码器和检测器往往没有联合优化，导致嵌入的信号可能不是检测器最容易识别的。
          *   **泛化能力有限**：面对复杂的攻击（如深度改写）时，性能仍有瓶颈。
  - 3. X-SIR (End-to-End SIR / Extended SIR) 算法
    X-SIR是SIR的进阶版本，代表了最新的研究趋势（如2025年ICML论文中提到的端到端模型）。它是目前鲁棒性和文本质量平衡最好的方案之一。

    *   **核心原理**：**端到端联合优化（End-to-End Joint Optimization）**。
        *   **联合训练**：X-SIR将水印嵌入模块（修改Logits）和水印检测模块作为一个整体进行端到端的训练。
        *   **可微代理**：由于LLM的采样过程（Sampling）是不可微的（无法直接反向传播梯度），X-SIR引入了特殊的技巧（如“在线提示”或使用可微的软采样近似），使得梯度可以从检测器的损失函数一直回传到嵌入模块。
        *   **自适应策略**：模型学会了在不同上下文中“隐藏”水印的最佳位置，使得水印信号极其隐蔽且难以被简单的统计攻击去除。
    *   **优点**：
        *   **极高的鲁棒性**：能够抵抗 paraphrase（复述）、translation（翻译）甚至部分删除攻击。
        *   **文本质量高**：由于是联合优化，模型学会了在不损害语义流畅度的前提下嵌入水印。
        *   **检测准确率高**：检测器是专门针对该嵌入方式训练的，比通用的统计检验更灵敏。
    *   **缺点**：需要额外的训练成本和计算资源，实现复杂度较高。
- 两种水印去除攻击方法
  - 1. Paraphrase（复述/改写攻击）
    *   **操作方式**：攻击者使用另一个大模型或专门的改写工具，对原始文本进行重新表述。这包括同义词替换、句式重组、主动被动语态转换、增减修饰语等，目的是保持原意但改变具体的用词和结构。
    *   **对水印的影响**：
        *   **对KGW**：致命打击。KGW依赖前后文的哈希来确定红绿名单。Paraphrase改变了Token序列，导致上下文哈希值完全变化，原本精心设计的绿名单Token被替换，水印信号瞬间瓦解。
        *   **对SIR/X-SIR**：有一定效果，但X-SIR由于经过了对抗训练或端到端优化，其水印信号往往隐含在更深层次的语义分布中，单纯的表面改写难以完全清除信号，检测器仍能发现异常统计特征。
  - 2. Translation（翻译攻击）
    *   **操作方式**：将生成的文本先翻译成另一种语言（如中文译成法语），然后再翻译回原始语言（法语译回中文）。这一过程通常被称为“回译”（Back-Translation）。
    *   **对水印的影响**：
        *   **原理**：翻译过程会彻底打乱原始的Token序列和句法结构。不同语言之间的映射不是一对一的，很多细微的统计特征会在翻译中丢失。
        *   **效果**：这是一种非常强大的攻击手段。对于基于固定规则（如KGW）的水印，回译几乎能100%去除水印。即使是较新的X-SIR算法，如果未经过专门的跨语言鲁棒性训练，也可能在剧烈的回译攻击下失效。不过，最新的端到端模型正在尝试通过多语言数据训练来增强对此类攻击的防御。

# 大模型安全

- 可解释的人工越狱模板
    - Pretending (伪装)：将模型带入某个场景环境，例如角色扮演、责任环境、研究实验；
    - Attention Shifting (注意力转移)：转换成另一种任务来得到输出，例如续写、翻译等；
    - Privilege Escalation (特权升级)：诱导模型打破限制，可以输出任何内容。

- ”侧”信道越狱方法
    - 加密编码：使用各种加密方式编码问题，然后输入到大模型中获取回复；
    - 低资源语言：将输入翻译成其他低资源语言然后输入到大模型中，并将回复翻译回常用语言；
    - 代码格式：将输入转换为代码形式让大模型计算并得到输出。

- 大模型隐写
  - 隐写术利用大模型巨大的参数空间或生成过程中的冗余度来藏匿数据：
  - 参数隐写：
    - 利用模型权重的低位比特（LSB）或特定的参数组合来编码二进制信息。由于大模型参数量巨大（数十亿），微小的改动不会影响模型智力，但能藏入大量数据。
  - 生成式隐写：
    - 控制模型生成的文本，使其表面看是正常对话，但特定的句式、选词或标点序列对应着秘密信息（类似传统的文本隐写，但由模型自动完成）。
  - 触发器隐写 (Backdoor-like)：
    - 训练模型在遇到特定“触发词”时输出预设的秘密信息，平时表现正常。这常被用于恶意目的，但也属于广义的隐写。

# 多模态大模型

- 语言
- 图像
- 音频
- 视频
- 点云数据

这里找一下最新的技术报告

# 智能体

基于大模型，构建能够动态感知环境，规划决策、使用工具、迭代演化的智能系统

- 具备自主和进化能力
	- 环境交互：感知理解、规划决策、动作落实
	- 持久运行：反思优化、迭代演化、安全防护
- 知识更新：知识库、经验库
- 集成工具：APP、API、函数
- GUI 智能体

## 智能体安全

- 内容安全风险
- 行为安全风险
  - 意图偏离，过度执行
  - 环境内容干扰
  - 恶意指令、工具后门
  - 社区知识操控
- 模型内生安全

- 风险建模
  - 可靠性风险：不存在有意针对智能体的攻击者，智能体在任务执行过程中无意造成风险
    - 知识缺失
    - 异常处置
    - 任务的复杂性和智能体能力的局限性（指令不完备、幻觉、任务规划缺陷、推理能力有限、安全意识不足等）
    - 在智能体与用户、环境的多轮交互过程中，即使没有恶意攻击，智能体也可能导致安全问题
  - 攻击风险：由有意攻击者引导智能体造成风险，可能来自用户侧和环境侧
    - 投毒攻击
    - 越狱攻击
    - 注入攻击
    - 对抗示例
    - 对抗扰动
  - 
