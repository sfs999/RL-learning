# 强化学习基础算法

## 多臂老虎机

<mark>交互不改变环境</mark>

### 累积懊悔

累积懊悔 (cumulative regret) 即操作$T$次拉杆后累积的懊悔总量，对于一次完整的$T$步决策$\{a_1, a_2, \ldots, a_T\}$，累积懊悔为$\sigma_R = \sum_{t=1}^{T}R(a_t)$。使得最大化累积奖励，等价于最小化累积懊悔。

### 期望逼近方法

采用增量式更新方法逐步逼近真实期望奖励。算法流程如下：

**初始化阶段：**
对于所有动作 $a \in \mathcal{A}$，初始化：
- 计数器：$N(a) = 0$
- 期望奖励估值：$\hat{Q}(a) = 0$

**迭代更新过程（$t = 1$ 到 $T$）：**

1. **选择动作**：选取某根拉杆，记为 $a_t$
2. **获得奖励**：执行动作后得到奖励 $r_t$
3. **更新计数器**：
   $$N(a_t) \leftarrow N(a_t) + 1$$
4. **更新期望奖励估值**：
   $$\hat{Q}(a_t) \leftarrow \hat{Q}(a_t) + \frac{1}{N(a_t)}[r_t - \hat{Q}(a_t)]$$

**公式推导：**

上述更新公式通过增量方式逐步逼近期望值。设 $Q_k$ 为前 $k$ 次奖励的平均值：

$$
\begin{aligned}
Q_k &= \frac{1}{k}\sum_{i=1}^{k}r_i \\
&= \frac{1}{k}\left(r_k + \sum_{i=1}^{k-1}r_i\right) \\
&= \frac{1}{k}(r_k + (k-1)Q_{k-1}) \\
&= \frac{1}{k}(r_k + kQ_{k-1} - Q_{k-1}) \\
&= Q_{k-1} + \frac{1}{k}[r_k - Q_{k-1}]
\end{aligned}
$$

该公式表明，新的估计值等于旧估计值加上步长参数 $\frac{1}{k}$ 乘以预测误差 $[r_k - Q_{k-1}]$。

### 主要算法
#### 策略迭代 (Policy Iteration)

交替进行策略评估和策略改进，直到策略收敛

##### 阶段一：策略评估 (Policy Evaluation)
- **目标**：计算当前固定策略 $\pi$ 的真实价值函数 $V^\pi$
- **迭代公式**：$V^{k+1}(s) \leftarrow \sum_{a} \pi(a|s) \left[ R(s,a) + \gamma \sum_{s'} P(s'|s,a) V^k(s') \right]$
- **核心逻辑**：严格按照策略 $\pi$ 指定的动作进行期望求和，通过反复迭代将未来回报累加回当前状态
- **信息传播过程**：从初始猜测（通常设为 0）开始，每一轮迭代将奖励信息向过去传播一步。$k=1$ 包含 1 步内的奖励，$k=2$ 包含 2 步内的累积奖励，$k\to\infty$ 时收敛到无穷步的真实值
- **停止条件**：当相邻两轮最大差值小于阈值 $\theta$ 时停止，即 $\max_s |V^{k+1}(s) - V^k(s)| < \theta$

##### 阶段二：策略改进 (Policy Improvement)
- **目标**：基于已算准的 $V^\pi$，找到更优策略 $\pi'$
- **更新公式**：$\pi'(s) = \arg\max_a \left[ R(s,a) + \gamma \sum_{s'} P(s'|s,a) V^\pi(s') \right]$
- **核心逻辑**：对每个状态遍历所有动作，选择能带来最大即时奖励加未来价值的动作
- **策略改进定理保证**：新策略一定优于或等于旧策略，即 $V^{\pi'}(s) \geq V^\pi(s)$

##### 算法特点
- 像"测绘员"：先测准一条路（策略评估），再换条更好的路测（策略改进）
- 通常迭代次数少，但单次迭代计算量大（需解线性方程组）
- 适用于状态空间较小、需要精确求解的场景

#### 价值迭代 (Value Iteration)
直接使用贝尔曼最优方程迭代，当价值函数 $V^*$ 收敛后提取最优策略 $\pi^*$

##### 目标
直接逼近全局最优策略的真实价值 $V^*$

##### 更新公式
$V_{k+1}(s) \leftarrow \max_a \left[ R(s,a) + \gamma \sum_{s'} P(s'|s,a) V_k(s') \right]$

##### 核心逻辑
- 每次更新价值时直接执行一次"策略提升"操作（使用 $\max$）
- 隐式地假设未来也会采取最优动作，不需要等待内部收敛
- 不是在算"当前策略"的价值，而是在算"理论上最高能得多少分"

##### 与策略迭代的本质区别
- **中间变量含义**：$V_k$ 逼近的是 $V^*$ 而非 $V^\pi$
- **核心算子**：使用 $\max$（贪婪地寻找理论上限）而非 $\sum$（模拟固定规则下的长期平均）
- **更新频率**：每次价值更新都包含一次隐式的策略提升，无需等待收敛

##### 算法特点
- 像"登山者"：每走一步都抬头看哪里最高，直接往那走
- 单次迭代计算量小，但可能需要更多次迭代才能收敛
- 对初值不敏感，无论初始值多大，根据压缩映射原理必收敛到 $V^*$

##### 策略提取
（仅在最后执行一次）：

$$\pi^*(s) = \arg\max_a \left[ R(s,a) + \gamma \sum_{s'} P(s'|s,a) V^*(s') \right]$$

#### 缺点：维数灾难 (Curse of Dimensionality)
- 每次迭代需遍历所有状态 $S$ 和动作 $A$，单次迭代复杂度约为 $O(|S|^2 |A|)$
- 当状态由多个变量组成时，状态空间随维度呈指数级膨胀。例如：10 个关节的机器人，每个关节 100 个角度，总状态数为 $100^{10} = 10^{20}$
- 这使得 DP 在高维连续状态空间（如图像输入、复杂机器人控制）中完全失效

> **规划而非学习**：智能体可以在不实际与环境交互的情况下，仅在脑海中进行"模拟"和计算，通过对贝尔曼方程的数学迭代，将全局最优解"传播"到每一个状态，从而推导出最优策略。这被称为**规划（Planning）**，区别于通过试错进行的**学习（Learning）**

### 多臂老虎机问题

**问题设定:**
- 有一个拥有 K 根拉杆的老虎机
- 每根拉杆对应一个未知的奖励概率分布
- 每次拉动一根拉杆，获得一个奖励 r

**目标:**
- 在操作 T 次拉杆后，获得尽可能高的累积奖励

$$\max \sum_{t=1}^{T} r_t, \quad r_t \sim \mathcal{R}(\cdot|a_t)$$

- 等价于最小化累计懊悔：

$$\min \sum_{t=1}^{T} R(a_t), \quad R(a_t) = \max_{a \in \mathcal{A}} \mathbb{E}[r|a] - \mathbb{E}[r|a_t]$$


**核心挑战:**
- 每根拉杆奖励的概率分布是未知的
- 需要在**探索**(尝试不同拉杆了解获奖概率) 和**利用**(根据经验选择获奖最多的拉杆) 之间进行权衡

### 策略算法

#### 1. ε-贪婪算法 (Epsilon-Greedy) <mark>盲目探索</mark>

**算法原理：**
- 以概率 $1-\epsilon$ 选择当前期望奖励估值最大的拉杆（利用）
- 以概率 $\epsilon$ 随机选择一根拉杆（探索）

**更新公式：**
$$\hat{Q}(a_t) \leftarrow \hat{Q}(a_t) + \frac{1}{N(a_t)}[r_t - \hat{Q}(a_t)]$$

**实验结果：**
- 50 臂老虎机，运行 50000 次，累积懊悔约为 **1316.46**
- 不同 $\epsilon$ 值的影响：
  - $\epsilon$越大，线性斜率越高

**特点分析：**
- ✅ 实现简单，计算效率高
- ✅ 能够持续探索，避免完全陷入局部最优
- ❌ $\epsilon$ 值固定，无法根据学习进程调整
- ❌ 探索效率较低，存在盲目性（探索强度恒定，找到最优解购仍继续高强度探索）

---

#### 2. 衰减ε-贪婪算法 (Decaying Epsilon-Greedy) <mark>探索欲只与时间相关</mark>

**算法原理：**
- $\epsilon$ 值随时间衰减：$\epsilon_t = \frac{1}{t}$
- 早期阶段：$\epsilon$ 较大，注重探索
- 后期阶段：$\epsilon$ 较小，注重利用

**实验结果：**
- 50 臂老虎机，运行 50000 次，累积懊悔约为 **1082.89**
- 相比固定$\epsilon=0.01$的贪婪算法，性能有所提升（约 **18%** 改进）

**特点分析：**
- ✅ 符合学习规律：先探索后利用
- ✅ 随着时间推移逐渐收敛到最优策略
- ✅ 累积懊悔增长速率随时间降低
- ❌ 衰减速度需要<mark>精心设计</mark>，过快或过慢都会影响性能
- ❌ 本次实验中拉杆较多，导致探索概率衰减速度过快，陷入局部最优解，当设定合适的衰减率，如$\epsilon_t =20* \frac{1}{t}$ 时累积懊悔为： 117.87

---

#### 3. 上置信界算法 (UCB - Upper Confidence Bound) <mark>探索欲综合考虑拉杆次数与经验期望并与时间相关</mark>

**算法原理：**

UCB 算法基于**霍夫丁不等式（Hoeffding's inequality**推导而来。

**1. 霍夫丁不等式：**

设 $X_1, \ldots, X_n$ 为 $n$ 个独立同分布的随机变量，取值范围为 $[0, 1]$，其经验期望为 $\bar{x}_n = \frac{1}{n}\sum_{j=1}^n X_j$，则有：

$$\mathbb{P}\{\mathbb{E}[X] \geq \bar{x}_n + u\} \leq e^{-2nu^2}$$

**2. 应用于多臂老虎机问题：**

将 $\hat{Q}_t(a)$ 代入 $\bar{x}_t$，令 $u = \hat{U}_t(a)$ 代表不确定性度量。给定概率 $p = e^{-2N_t(a)U_t(a)^2}$，根据霍夫丁不等式，真实期望奖励 $Q_t(a) < \hat{Q}_t(a) + \hat{U}_t(a)$ 至少以概率 $1 - p$ 成立。

**3. 不确定性度量推导：**

由 $p = e^{-2N_t(a)U_t(a)^2}$ 解得：

$$\hat{U}_t(a) = \sqrt{\frac{-\ln p}{2N_t(a)}}$$

**4. UCB 公式：**

设定 $p = \frac{1}{t}$（随时间减小），并在分母中加上常数 1 避免除零，同时引入系数 $c$ 控制探索比重：

$$UCB_t(a) = \hat{Q}_t(a) + c\sqrt{\frac{\ln t}{N_t(a)}}$$

- 第一项：期望奖励估值（利用项）
- 第二项：不确定性度量（探索项），与尝试次数成反比

拉动次数越少，探索概率越高，探索概率由拉动次数和经验期望共同决定，经验期望大的拉杆 ucb 也大，但随着其他拉杆多次未探索，未探索的拉杆由于$\sqrt{\frac{\ln t}{N_t(a)}}$增大，所以 ucb 也会增大，但随着时间推移 t 减小$\sqrt{\frac{\ln t}{N_t(a)}}$也会跟着减小

**实验结果：**
- 50 臂老虎机，运行 50000 次，累积懊悔约为 **799.01**（当$c=1$时）
- 系数 $c$ 的影响：
  - $c$ 较大：过度探索不确定性高的拉杆
  - $c$ 较小：探索不足，可能错过潜在优质拉杆

**特点分析：**
- ✅ 基于乐观原则，自动平衡探索与利用
- ✅ 不需要随机探索，具有确定性
- ✅ 对未充分探索的拉杆给予更高优先级
- ❌ 对数增长项可能导致过度探索
- ❌ 需要调节系数 $c$，对性能影响较大（这里的 c 就是调节探索欲望）

---

#### 4. 汤普森采样算法 (Thompson Sampling)

**算法原理：**
- 为每个拉杆维护一个 Beta 分布 $Beta(\alpha_a, \beta_a)$
  - $\alpha_a$：拉杆 $a$ 获得奖励为 1 的次数
  - $\beta_a$：拉杆 $a$ 获得奖励为 0 的次数
- 每次从每个拉杆的 Beta 分布中采样，选择采样值最大的拉杆

$\alpha_a$ 越大，采样到大值的概率越高
$\beta_a$ 越大，采样到大值的概率越低

**更新规则：**
$$\begin{aligned}\alpha_a &\leftarrow \alpha_a + r \\ \beta_a &\leftarrow \beta_a + (1 - r)\end{aligned}$$

**实验结果：**
- 50 臂老虎机，运行 50000 次，累积懊悔约为 **118.40**

**特点分析：**
- ✅ 基于贝叶斯框架，具有坚实的理论基础
- ✅ 自动根据不确定性调整探索程度
- ✅ 实现简单，无需调节超参数
- ✅ 在实践中表现优异，常优于 UCB
- ❌ 需要维护概率分布，计算量略大
- ❌ 采样过程引入随机性，结果不够稳定

**蒙特卡洛方法 (Monte Carlo Methods)** <mark>需要完整模拟所有路径，即知道执行结果反推轨迹价值</mark>
  - **核心机制：无需模型，基于采样**
    - 完全不需要知道环境的数学模型（$P$ 和 $R$），只关心**实际发生了什么**
    - **如何估算价值**：基于大数定律，通过大量采样来逼近期望值
      - 智能体与环境交互，记录从开始到结束的完整过程（称为**轨迹**或**Episode**）
      - 对于某个状态 $s$，统计所有访问过该状态的轨迹，计算这些轨迹中从 $s$ 开始直到结束所获得的**实际回报（Return, $G_t$）的平均值**
      - 公式：$V(s) \approx \frac{1}{N(s)} \sum_{i=1}^{N(s)} G_t^{(i)}$，其中 $N(s)$ 是访问次数，$G_t$ 是实际获得的累积折扣奖励
    - **与 DP 的本质区别**：MC 使用**真实发生的后续回报**更新价值，而不是用“下一步的估计值”更新“当前值”（即**没有自举 Bootstrapping**）
  
  - **为什么必须是回合制任务 (Episodic Tasks)**
    - **回合制任务的定义**：有明确的**开始状态**和**终止状态**的任务
      - 例子：一局围棋（从落子开始到分出胜负）、走迷宫（从入口到出口或撞墙）、一次机器人抓取尝试
      - 时间步 $t$ 会在某个时刻 $T$ 终止，然后环境重置，开始新的回合
    - **MC 依赖完整回报 $G_t$**：
      $$G_t = R_{t+1} + \gamma R_{t+2} + \dots + \gamma^{T-1} R_T$$
      注意公式中的 $T$（终止时刻）。MC **必须**等到一个回合彻底结束（游戏通关、游戏失败、任务完成），才能计算出完整的 $G_t$
    - **持续性任务的困境**：如果任务没有终点（如控制恒温器永远运行），则 $T \to \infty$，无法计算完整的 $G_t$，MC 方法失效
  
  - **优缺点分析**
    - **优点**：
      - 简单直观，逻辑符合人类直觉（"试得多了，平均结果就是真实水平"）
      - 无需建模，可直接应用于黑盒环境
      - 无偏差收敛：只要采样次数足够多，保证收敛到真实的价值函数 $V_\pi$
      - 并行友好：每个回合独立，易于并行生成大量轨迹加速学习
    - **缺点**：
      - **高方差**：依赖单次采样的完整轨迹，运气好坏会导致价值估计波动大
      - **只能用于回合制**：无法直接处理没有终点的长期持续任务
      - **样本效率低**：必须等到回合结束才能学习，学习信号延迟严重
      - **探索问题**：从不访问的状态永远无法更新（需结合 $\epsilon$-greedy 等探索策略）
  
  > **实际应用示例**：训练 AI 玩《超级马里奥》时，MC 让 AI 随机玩一局游戏并记录所有步骤，游戏结束后计算总分，回溯这一局经过的所有状态并根据实际总分更新这些状态的价值。重复玩几千局后，平均值越来越接近真实的通关概率或得分期望。

**时序差分学习 (Temporal Difference, TD)** <mark>根据当前的观察与下一步的预测，更新当前步骤的评分，影响下一次智能体遇到步骤时的选择</mark>
  - **核心思想：DP 与 MC 的"完美混血儿"**
    - **像 MC 一样无需模型 (Model-Free)**：不需要知道环境的状态转移概率 $P$ 和奖励函数 $R$，完全通过与环境的实际交互（采样）来学习，可直接应用于黑盒环境
    - **像 DP 一样自举 (Bootstrapping)**：不需要等到回合结束，在每一步利用当前对下一步价值的估计来更新当前的价值。依赖的是即时奖励 + 下一状态的估计价值 $(R_{t+1} + \gamma V(S_{t+1}))$，而非真实的完整回报 $G_t$
  
  - **核心机制：TD 误差 (TD Error)**
    - TD 学习的更新动力来自于衡量"现在的预测"和"更好的预测"之间的差距
    - 状态价值函数更新公式：
      $$V(S_t) \leftarrow V(S_t) + \alpha \cdot [R_{t+1} + \gamma V(S_{t+1}) - V(S_t)]$$
      其中 TD 误差 $\delta_t = R_{t+1} + \gamma V(S_{t+1}) - V(S_t)$
    - **直观理解**：$R_{t+1} + \gamma V(S_{t+1})$ 是 TD 目标（基于最新观察构建的更准确估计），$V(S_t)$ 是当前旧估计，$\alpha$ 是学习率。每走一步就可以立即更新上一步的认知，无需等待最终结果
      > 强化学习中奖励符号主要有两种定义流派：**Sutton & Barto 体系**（主流）将奖励视为动作的因果结果，记为 $R_{t+1}$，强调在 $t$ 时刻执行动作后于 $t+1$ 时刻收到奖励，其下标与新状态 $S_{t+1}$ 对齐，逻辑严谨；而**经典控制或旧版教材体系**则倾向于将奖励视为 $t$ 时刻的即时反馈，记为 $r_t$。两者本质描述的是同一物理过程，仅时间下标相差 1（即 $r_t \equiv R_{t+1}$），在使用时只需保持上下文符号一致即可。
      > 
      > 从价值函数定义出发，使用 $r_t$ 记号的完整推导过程如下：
      > 
      >  $$
      >  \begin{aligned}
      >  V_\pi(s) &= \mathbb{E}_\pi \left[ \sum_{k=0}^{\infty} \gamma^k r_{t+k} \;\middle|\; S_t = s \right] & (\text{定义}) \\
      >  &= \mathbb{E}_\pi \left[ r_t + \sum_{k=1}^{\infty} \gamma^k r_{t+k} \;\middle|\; S_t = s \right] & (\text{拆分 } k=0 \text{ 项}) \\
      >  &= \mathbb{E}_\pi \left[ r_t + \gamma \sum_{k=1}^{\infty} \gamma^{k-1} r_{t+k} \;\middle|\; S_t = s \right] & (\text{提取公因子 } \gamma) \\
      >  &= \mathbb{E}_\pi \left[ r_t + \gamma \sum_{j=0}^{\infty} \gamma^{j} r_{(t+1)+j} \;\middle|\; S_t = s \right] & (\text{变量代换 } j=k-1) \\
      >  &= \mathbb{E}_\pi \left[ r_t + \gamma G_{t+1} \;\middle|\; S_t = s \right] & (\text{识别 } G_{t+1}) \\
      >  &= \mathbb{E}_\pi \left[ r_t + \gamma \mathbb{E}_\pi [G_{t+1} \mid S_{t+1}] \;\middle|\; S_t = s \right] & (\text{引入条件期望}) \\
      >  &= \mathbb{E}_\pi \left[ r_t + \gamma V_\pi(S_{t+1}) \;\middle|\; S_t = s \right] & (\text{代入价值函数定义})
      >  \end{aligned}
      >  $$
      >
      > 最终得到贝尔曼期望方程：
      > $$ V_\pi(s) = \mathbb{E}_\pi [ r_t + \gamma V_\pi(S_{t+1}) \mid S_t = s ] $$
      >
      > 获取 $r_{t}$ ，查询状态 $S_{t+1}$ 来更新 $S_{t}$

  - **代表算法**
    - **(1) SARSA (On-Policy / 同策略)**
      - **名字来源**：State, Action, Reward, Next State, Next Action
      - **核心思想**：学习的是当前正在执行的策略的价值
      - **更新公式**：$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha [R_{t+1} + \gamma Q(S_{t+1}, A_{t+1}) - Q(S_t, A_t)]$
      - **策略选择**：使用ε-贪婪算法进行状态选择，$Q(S_{t+1}, A_{t+1}) \leftarrow S_{t+1} \leftarrow argmax_{a}Q(S_{t}, A_{t})$
      - **特点**：使用智能体在 $S_{t+1}$ 实际选择的动作 $A_{t+1}$ 的 $Q$ 值
      - **On-Policy**：行为策略和目标策略必须是同一个
      - **优势**：更保守、更安全，考虑了探索带来的风险
      - **缺点**：收敛到的策略依赖于探索方式
    - **(2) 多步时序差分算法**：
      - **基本思想**：使用 n 步的奖励，然后使用之后状态的价值估计，结合蒙特卡洛方法和单步时序差分的优势
      - **n 步回报定义**：$G_t = r_t + \gamma r_{t+1} + \cdots + \gamma^{n-1}r_{t+n-1} + \gamma^n Q(S_{t+n}, A_{t+n})$
      - **多步 SARSA 更新公式**：$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha [G_t - Q(S_t, A_t)]$
      - **安全性考虑**：
        - 多步 SARSA 在更长的时间范围内考虑实际采取的动作序列，能够捕捉更长远的奖励信号
        - 通过考虑未来 n 步的实际动作和奖励，更准确地估计探索行为的真实后果
        - 相比单步 SARSA，多步方法能更好地评估长期风险，避免因短视而忽略潜在的负面后果
        - 在安全敏感场景中，多步 SARSA 能够提前预见多步之后的潜在危险状态，从而采取更保守的策略
        - 随着 n 增大，算法逐渐接近蒙特卡洛方法，无偏性增强但方差增大，需要在偏差 - 方差权衡中选择合适的 n 值
      - **特点**：n 步 SARSA 仍然是同策略算法，目标策略和行为策略相同，都使用ε-贪婪策略
    - **(3) Q-Learning (Off-Policy / 异策略)**
      - **核心思想**：学习最优策略的价值，不管智能体当前实际上是怎么探索的
      - **更新公式**：$Q(S_t, A_t) \leftarrow Q(S_t, A_t) + \alpha [R_{t+1} + \gamma \max_a Q(S_{t+1}, a) - Q(S_t, A_t)]$
      - **特点**：计算 TD 目标时假设下一步会选择能让 $Q$ 值最大的动作，即使实际探索时选了别的动作（如 $\epsilon$-greedy 随机探索）
      - **Off-Policy**：<mark>行为策略（用来探索的）和目标策略（用来学习的）可以不同</mark> 更新时用的和实际执行时用的是两码事
      - **优势**：倾向于直接学习最优路径，是 Deep Q-Network (DQN) 的基础
      - **缺点**：在噪声大的环境中可能存在最大化偏差 (Maximization Bias)，高估价值
    - **(4) Dyna-Q**
      - **核心思想**：结合基于模型的学习和无模型学习，通过真实交互数据学习环境模型，再利用模型生成虚拟经验进行规划，从而加速学习
      - **与 Q-Learning 的区别**：
        - Q-Learning：仅通过真实交互数据 $(s, a, r, s')$ 更新 $Q$ 值
        - Dyna-Q：在 Q-Learning 基础上增加了**模型学习**和**规划**两个环节
          1. **学习模型**：用真实交互数据 $(s, a, r, s')$ 学习环境模型 $M(s, a) \rightarrow r, s'$
          2. **规划更新**：每次真实交互后，额外进行 $N$ 次"虚拟"学习——从历史经验中随机采样 $(s_m, a_m)$，用模型预测结果 $(r_m, s'_m)$ 来更新 $Q$ 值
        - 关键参数 $N$：规划次数，$N=0$ 时退化为普通 Q-Learning
      - **特点**：每次真实交互后执行 $N$ 次 Q-planning，$N$ 为可调超参数；当 $N=0$ 时退化为普通 Q-learning
      - **适用环境**：离散且确定的环境，可以直接用经验数据 $(s, a, r, s')$ 更新模型

    - **场景举例：悬崖行走 (Cliff Walking)**
      - 环境：网格世界，起点左下，终点右下，中间有"悬崖"，掉下去奖励 -100，每步奖励 -1
      - **Q-Learning**：学到紧贴悬崖边缘的最短路径（理论上最优），但训练时会因随机探索经常掉下悬崖
      - **SARSA**：学到远离悬崖的安全路径，因为它考虑了"我下一步可能会随机乱走掉下去"的风险
  
  > 直观理解：
  >
  > 如果你原本以为这个状态值 10 分 (V(S_t)=10)。
  >
  > 走一步后，你拿到了 2 分奖励 (R=2)，且发现下一个状态看起来值 9 分 (V(S_{t+1})=9)，折扣因子 gamma=1。
  >
  > 那么新的目标值是 2 + 9 = 11。
  >
  > TD 误差是 11 - 10 = +1。
  >
  > 你会把原估计值往 11 的方向调整一点（比如调整 0.1，变成 10.1）。

**深度强化学习 (Deep Reinforcement Learning, Deep RL)** <mark>将决策经验训练到神经网络中，然后代替决策函数进行决策</mark>
  - **核心驱动力：为什么需要"深度"？**
    - **传统表格型方法的困境**：当状态空间极大时，无法创建存储所有状态的表格
      - 围棋：状态数约 $10^{170}$
      - Atari 游戏图像（$84 \times 84$ 像素）：状态数约 $256^{7056}$，比宇宙原子总数还大
      - **结论**：不可能遍历所有状态来更新它们
    
    - **深度学习的解决方案：函数近似 (Function Approximation)**
      - 不再存储每个状态的具体数值，而是训练深度神经网络，参数为 $\theta$
      - 网络作为函数拟合器：$Q(s, a; \theta) \approx Q^*(s, a)$ 或 $\pi(a|s; \theta) \approx \pi^*(a|s)$
      - **泛化能力 (Generalization)**：最关键的优势。如果见过"左上角有个敌人"，遇到"左上角稍微偏下的敌人"时，即使从未见过这个确切状态，也能根据相似性推断出大致的价值或策略
      - 这使得处理连续、高维状态成为可能
  
  - **两大主流流派**
    - **(A) 基于价值的方法 (Value-Based Methods)** <mark>衡量某个动作的未来得分</mark>
      - **目标**：用神经网络近似动作价值函数 $Q(s, a)$
      - **策略获取**：通过贪心策略选择 $Q$ 值最大的动作 ($\arg\max_a Q(s, a)$)
      - **代表算法**：**DQN (Deep Q-Network)** 及其变体 (Double DQN, Dueling DQN)
      - **特点**：通常只能处理离散动作空间；训练相对稳定，但可能收敛到次优解
    
    - **(B) 基于策略的方法 (Policy-Based Methods)** <mark>衡量各个动作的选择概率</mark>
      - **目标**：用神经网络直接近似策略函数 $\pi(a|s)$，输出动作的概率分布
      - **价值获取**：通常学习一个辅助的价值函数 $V(s)$ 来减少方差（即 Actor-Critic 架构）
      - **代表算法**：**PPO (Proximal Policy Optimization)**, A3C, TRPO
      - **特点**：可以直接处理**连续动作空间**（如机器人关节角度、方向盘转角）；能够学习随机策略；通常收敛更快，但训练可能不稳定
    
    - **(C) 演员 - 评论家架构 (Actor-Critic)**
      - 结合上述两者：
        - **Actor (演员)**：策略网络，负责根据状态输出动作
        - **Critic (评论家)**：价值网络，负责评估 Actor 的动作好不好（计算 TD Error），指导 Actor 更新
      - PPO 和 A3C 都属于这一类
  
  - **代表算法详解**
    - **DQN (Deep Q-Network) - 里程碑之作**
      - **背景**：2015 年 DeepMind 提出，首次证明 RL+DL 可以在 Atari 游戏上达到超越人类的水平
      - **核心机制**：
        - 输入：原始像素图像
        - 输出：每个可能动作的 Q 值
        - **两大创新技巧**（解决训练不稳定的关键）：
          - **经验回放 (Experience Replay)**：将智能体的经历 $(s, a, r, s')$ 存入缓冲区，训练时随机采样。打破数据间的相关性，使数据更像独立同分布 (IID)，适合深度学习
          - **目标网络 (Target Network)**：使用两个网络，一个用于预测 (Online Net)，一个用于计算目标值 (Target Net)。Target Net 的参数定期从 Online Net 复制，保持短期稳定，避免"追逐自己的尾巴"导致的发散
      - **损失函数与残差**：
        - **Q-learning 更新规则**：$Q(s, a) \leftarrow Q(s, a) + \alpha \left[ r + \gamma \max_{a' \in A} Q(s', a') - Q(s, a) \right]$
        - **TD 目标**：$r + \gamma \max_{a' \in A} Q(s', a')$
        - **残差（TD Error）**：$\delta = r + \gamma \max_{a' \in A} Q(s', a') - Q(s, a)$
        - **损失函数**：$\omega^* = \arg\min_{\omega} \frac{1}{2N} \sum_{i=1}^{N} \left[ Q_{\omega}(s_i, a_i) - \left( r_i + \gamma \max_{a'} Q_{\omega}(s_i', a') \right) \right]^2$  ($\frac{1}{2N}$ 中的 2 用来抵消平方求导产生的 2，让梯度表达式更简洁)
      - **局限**：只能处理<mark>离散</mark>动作；容易高估 Q 值
      - 当以图像为输入时，DQN 网络通常会将最近的几帧图像一起作为输入，从而感知环境的动态性，采样时按照一帧来采样，但是模型的输入是将相邻多帧进行打包输入的，依次来提供物体运动趋势
      - **算法流程**：
        - 用随机的网络参数$\omega$初始化网络$Q_\omega(s, a)$
        - 复制相同的参数$\omega^- \leftarrow \omega$来初始化目标网络$Q_{\omega^-}$
        - 初始化经验回放池$R$
        - **for** 序列$e = 1 \rightarrow E$ **do**
          - 获取环境初始状态$s_1$
          - **for** 时间步$t = 1 \rightarrow T$ **do**
            - 根据当前网络$Q_\omega(s, a)$以$\epsilon$-贪婪策略选择动作$a_t$
            - 执行动作$a_t$，获得回报$r_t$，环境状态变为$s_{t+1}$
            - 将$(s_t, a_t, r_t, s_{t+1})$存储进回放池$R$中
            - 若$R$中数据足够，从$R$中采样$N$个数据$\{(s_i, a_i, r_i, s_{i+1})\}_{i=1,\ldots,N}$
            - 对每个数据，用目标网络计算$y_i = r_i + \gamma \max_{a} Q_{\omega^-}(s_{i+1}, a)$
            - 最小化目标损失$L = \frac{1}{N} \sum_{i}(y_i - Q_\omega(s_i, a_i))^2$，以此更新当前网络$Q_\omega$
            - 更新目标网络
          - **end for**
        - **end for**
      - 改进算法
        - Double DQN
          - 为避免 DQN 算法导致的 Q 被过高估计的累计误差
            - 由于神经网络估计不可避免地存在噪声或误差，$\max$ 操作会系统性地挑选出那些因随机正向噪声而被偶然高估的动作值，而随后又用同一个网络的该高估值作为目标进行更新，这种“自己选、自己评”的机制缺乏误差校正，导致正向偏差被锁定并通过自举（Bootstrapping）不断累积和放大，最终使智能体对状态价值产生盲目乐观的误判。
          - 在计算用$y_i = r_i + \gamma \max_{a} Q_{\omega^-}(s_{i+1}, a)$时
          - 使用训练网络选取$a^* = argmax_{a}Q_{\omega}(s_{i+1}, a)$
          - 之后在使用目标网络计算$y_i$
        - Dueling DQN
          - **核心思想**：将 Q 值分解为<mark>状态</mark>价值 V(s) 和<mark>动作优势</mark>函数 A(s,a) 两部分，通过不同的网络分支分别学习 
          - **网络结构创新**：
            - 共享特征提取层（卷积层或全连接层）
            - 分为两个独立输出分支：
              - **价值分支 (Value Stream)**：输出状态价值$V(s; \omega, \alpha)$，表示处于某个状态的好坏程度
              - **优势分支 (Advantage Stream)**：输出动作优势$A(s,a; \omega, \beta)$，表示相对于其他动作，选择某个动作的优势有多大
          - **Q 值聚合方式**：$Q(s,a; \omega, \alpha, \beta) = V(s; \omega, \alpha) + A(s,a; \omega, \beta)$
          - **关键技巧 - 优势函数中心化**：
            - 问题：直接相加会导致网络无法唯一确定 V 和 A（恒等变换问题）
            - 解决方案：$Q(s,a; \omega, \alpha, \beta) = V(s; \omega, \alpha) + \left(A(s,a; \omega, \beta) - \frac{1}{|A|}\sum_{a'}A(s,a'; \omega, \beta)\right)$
            - 作用：强制优势函数的平均值为 0，使得 V(s) 真正代表状态的绝对价值
          - **优势**：
            - 能够学习到哪些状态是真正重要的，而不需要为每个动作都学习 Q 值
            - 在某些状态下，即使动作改变，Q 值也不会变化很大（优势接近 0）
            - 提高训练效率和最终性能，尤其在高维动作空间中效果显著
          - **与 Double DQN 结合**：Dueling 只是一种网络架构改进，可以与 Double DQN等技术同时使用

    - **REINFORCE** 
      - **背景**：最经典的蒙特卡洛策略梯度算法
      - **理论基础：策略梯度定理 (Policy Gradient Theorem)**
        - **目标函数**：$J(\theta) = \mathbb{E}_{s_0}[V^{\pi_\theta}(s_0)]$，表示在策略 $\pi_\theta$ 下从初始状态出发的期望累积回报
        - **策略梯度定理**：
          $$
          \nabla_\theta J(\theta) = \mathbb{E}_{\pi_\theta}\left[Q^{\pi_\theta}(s, a) \nabla_\theta \log \pi_\theta(a|s)\right]
          $$
        - **推导核心思想**：
          - 对状态价值函数 $V^{\pi_\theta}(s)$ 求梯度，通过贝尔曼方程递归展开
          - 引入状态访问分布 $\nu^{\pi_\theta}(s)$：表示在策略 $\pi_\theta$ 下访问状态 $s$ 的频率
          - 最终形式：$\nabla_\theta J(\theta) \propto \sum_{s} \nu^{\pi_\theta}(s) \sum_{a} Q^{\pi_\theta}(s,a) \nabla_\theta \pi_\theta(a|s)$
          - 通过乘除 $\pi_\theta(a|s)$ 并利用 $\nabla_\theta \log f(\theta) = \frac{\nabla_\theta f(\theta)}{f(\theta)}$，转化为期望形式
        - **物理意义**：
          - 梯度更新天然关注"常去的状态"——高频访问状态自动获得更大权重
          - 在每个状态下，根据动作的优劣程度 $Q(s,a)$ 加权调整策略
          - $\nabla_\theta \log \pi_\theta(a|s)$ 称为 score function，表示参数如何变化能让动作 $a$ 更易被选中
      - **核心机制**：
        - 使用蒙特卡洛方法估计 $Q^{\pi_\theta}(s, a) \approx G_t$（从时刻 t 到 episode 结束的实际回报）
        - 用采样轨迹近似期望 $\mathbb{E}_{\pi_\theta}[\cdot]$，得到无偏样本估计
        - 策略梯度公式：
          $$
          \nabla_\theta J(\theta) = \mathbb{E}_{\pi_\theta}\left[\sum_{t=0}^{T}\left(\sum_{t'=t}^{T}\gamma^{t'-t}r_{t'}\right)\nabla_\theta \log \pi_\theta(a_t|s_t)\right]
          $$
        - 其中 $T$ 是和环境交互的最大步数（例如车杆环境中 $T = 200$）
      - **算法流程**：
        - 初始化策略参数 $\theta$
        - for 序列 $e = 1 \rightarrow E$ do:
          - 用当前策略 $\pi_\theta$ 采样轨迹 $\{s_1, a_1, r_1, s_2, a_2, r_2, \ldots s_T, a_T, r_T\}$
          - 计算当前轨迹每个时刻 $t$ 往后的回报 $\sum_{t'=t}^{T}\gamma^{t'-t}r_{t'}$，记为 $\psi_t$（即 $G_t$）
          - 对 $\theta$ 进行更新：$\theta = \theta + \alpha \sum_{t}^{T}\psi_t\nabla_\theta \log \pi_\theta(a_t|s_t)$
        - end for
      - **原理**：
        - 策略参数化，以神经网络训练的方式训练该参数化策略
        - 每轮训练之后通过蒙特卡洛模拟重新评估状态的价值
        - REINFORCE 的更新规则 $\theta \leftarrow \theta + \alpha G_t \nabla_\theta \log \pi_\theta(a_t|s_t)$ 正是策略梯度定理的一个无偏样本估计
      - **特点**：
        - 无偏估计（使用真实回报）
        - 高方差（需要完整轨迹，方差较大）
        - 仅适用于回合制任务
    
    - **Actor-Critic**
      - **背景**：为克服 REINFORCE 算法的高方差问题而提出，结合了基于值函数和基于策略梯度的方法的优势
      - **核心思想**：
        - 引入两个网络：Actor（演员）和 Critic（评论家）
        - **Actor**：负责根据状态选择动作，即策略网络 $\pi_\theta(a|s)$，参数为 $\theta$
        - **Critic**：负责评估 Actor 选择的动作有多好，即值函数网络 $V_\omega(s)$ 或 $Q_\omega(s,a)$，参数为 $\omega$
      - **理论基础**：
        - 在策略梯度公式中，权重项 $\psi_t$ 可以采用多种形式，其中优势函数 $A^{\pi_\theta}(s_t, a_t)$ 是最优选择
        - 优势函数定义：$A^{\pi}(s, a) = Q^{\pi}(s, a) - V^{\pi}(s)$，表示在状态 $s$ 下选择动作 $a$ 相对于平均表现的优越程度
        - 策略梯度更新公式：$\nabla_\theta J(\theta) = \mathbb{E}_{\pi_\theta}\left[A^{\pi_\theta}(s, a) \nabla_\theta \log \pi_\theta(a|s)\right]$
        - 使用优势函数代替原始回报 $G_t$，可以在保持梯度方向正确性的同时大幅降低方差
      - **时序差分学习**：
        - Critic 网络通过时序差分（TD）方法学习估计状态价值函数 $V_\omega(s)$
        - TD 误差：$\delta_t = r_t + \gamma V_\omega(s_{t+1}) - V_\omega(s_t)$
        - TD 误差 $\delta_t$ 作为优势函数 $A(s_t, a_t)$ 的无偏估计
        - Actor 的更新规则：$\theta \leftarrow \theta + \alpha \delta_t \nabla_\theta \log \pi_\theta(a_t|s_t)$
      - **算法流程（两种实现方式）**：
        
        **方式一：回合制批量更新（Episode-batch Update）**
        - 初始化 Actor 参数 $\theta$ 和 Critic 参数 $\omega$
        - for 每个回合 $e = 1 \rightarrow E$ do:
          - 用当前策略 $\pi_\theta$ 采样完整轨迹 $\{s_1, a_1, r_1, s_2, a_2, r_2, \dots\}$
          - for 轨迹中每一步 $t$ do:
            - 计算 TD 误差：$\delta_t = r_t + \gamma V_\omega(s_{t+1}) - V_\omega(s_t)$
          - end for
          - **批量更新** Critic 参数：$\omega \leftarrow \omega + \beta \sum_t \delta_t \nabla_\omega V_\omega(s_t)$
          - **批量更新** Actor 参数：$\theta \leftarrow \theta + \alpha \sum_t \delta_t \nabla_\theta \log \pi_\theta(a_t|s_t)$
        - end for
        - **特点**：
          - 参数更新频率：每个回合结束后更新一次
          - 梯度方向更稳定（整条轨迹的梯度平均）
          - 适用于回合制任务、教学演示、短 episode 场景（如 CartPole、Atari 游戏）
        
        **方式二：单步在线更新（Step-by-step Online Update）**
        - 初始化 Actor 参数 $\theta$ 和 Critic 参数 $\omega$
        - 初始化状态 $s_1$
        - for 每个时间步 $t = 1 \rightarrow T$ do:
          - Actor 根据当前策略 $\pi_\theta(a_t|s_t)$ 选择动作 $a_t$
          - 执行动作 $a_t$，观测奖励 $r_t$ 和下一状态 $s_{t+1}$
          - Critic 计算 TD 误差：$\delta_t = r_t + \gamma V_\omega(s_{t+1}) - V_\omega(s_t)$（若终止则 $V_\omega(s_{t+1}) = 0$）
          - **立即更新** Critic 参数：$\omega \leftarrow \omega + \beta \delta_t \nabla_\omega V_\omega(s_t)$
          - **立即更新** Actor 参数：$\theta \leftarrow \theta + \alpha \delta_t \nabla_\theta \log \pi_\theta(a_t|s_t)$
          - 更新状态：$s_t \leftarrow s_{t+1}$
          - 若任务终止则重新开始或结束
        - end for
        - **特点**：
          - 参数更新频率：每一步交互后立即更新
          - 数据效率高（可流式处理，无需存储完整轨迹）
          - 适用于连续控制任务、Off-policy 算法、大规模训练（如 A3C、SAC 等）
          - 单步噪声影响较大，但可通过学习率调节
        
        **两种方式的核心区别**：
        - **更新时机**：方式一在 episode 结束后累积梯度再更新；方式二在每一步交互后立即更新
        - **数据利用**：方式一需存储完整轨迹；方式二可实时处理，内存占用更低
        - **稳定性**：方式一梯度方向更稳定；方式二波动较大但更灵活
        - **应用场景**：方式一适合教学、离散任务；方式二适合连续控制、工程实践
      - **优势**：
        - **低方差**：通过引入 Critic 网络估计优势函数，避免了 REINFORCE 使用蒙特卡洛回报导致的高方差
        - **在线学习**：单步在线更新版本每一步都可以实时更新，无需等待完整轨迹，适用于连续任务；回合制版本则需在 episode 结束后批量更新
        - **样本效率**：可以利用 bootstrapping（自举）方法，从部分轨迹中学习，尤其是单步更新版本可流式处理数据
        - **稳定性**：Critic 提供的连续反馈信号使训练过程更加稳定，回合制批量更新的梯度方向更稳定，单步更新更灵活但波动稍大
      - **与 REINFORCE 对比**：
        - REINFORCE 使用蒙特卡洛方法估计 $Q(s,a) \approx G_t$，无偏但方差大
        - Actor-Critic 使用时序差分方法估计优势函数 $A(s,a) \approx \delta_t$，有偏但方差小
        - REINFORCE 仅适用于回合制任务，Actor-Critic 适用于连续和回合制任务
        - Actor-Critic 通过引入基线函数 $b(s_t) = V_\omega(s_t)$，在梯度估计中减去基线，进一步降低方差

    - **TRPO**
      - 基于策略的方法在优化时会由于步长较长导致策略突然变差，所以需要限定策略梯度算法的更新范围，<mark>相比Actor-Critic对策略网络进行更新时的梯度进行了限制</mark>
      - **核心思想**：通过约束新策略 theta' 与旧策略 theta 之间的 KL 散度，确保策略更新幅度不会过大，从而保证策略性能单调递增
      - **数学推导**：
        - **策略性能定义**：$J(\theta) = \mathbb{E}_{s_0}[V^{\theta}(s_0)]$，表示从初始状态分布出发的期望回报
        - **裂项求和恒等式**（关键技巧）：
          $$
          V^{\theta}(s_0) = \sum_{t=0}^{\infty} \gamma^t \left( V^{\theta}(s_t) - \gamma V^{\theta}(s_{t+1}) \right)
          $$
          证明：
          $$
          \begin{aligned}
          &\sum_{t=0}^{\infty} \gamma^t V^{\theta}(s_t) - \sum_{t=1}^{\infty} \gamma^t V^{\theta}(s_t) \\
          &= V^{\theta}(s_0) + \sum_{t=1}^{\infty} \gamma^t V^{\theta}(s_t) - \sum_{t=1}^{\infty} \gamma^t V^{\theta}(s_t) \\
          &= V^{\theta}(s_0)
          \end{aligned}
          $$
        - **期望形式转换**：两边取期望，若轨迹由旧策略 $\theta$ 生成，则
          $$
          J(\theta') = \mathbb{E}_{\theta}\left[ \sum_{t=0}^{\infty} \gamma^t \left( V^{\theta'}(s_t) - \gamma V^{\theta'}(s_{t+1}) \right) \right]
          $$
          注意：这里期望下标是 $\theta$（旧策略），而非 $\theta'$（新策略）。在实际算法中，我们用旧策略采样的数据来估计新策略性能。
        - **引入优势函数**：
          - 定义 TD error 形式：$\delta_t = r(s_t, a_t) + \gamma V^{\theta'}(s_{t+1}) - V^{\theta'}(s_t)$
          - 优势函数：$A(s_t, a_t) = Q^{\theta'}(s_t, a_t) - V^{\theta'}(s_t) = \mathbb{E}[\delta_t]$
        - **策略性能改进量**：新旧策略目标函数差距
          $$
          J(\theta') - J(\theta) = \mathbb{E}_{\theta}\left[\sum_{t=0}^{\infty} \gamma^t A^{\theta'}(s_t, a_t) \frac{\theta'(a_t|s_t)}{\theta(a_t|s_t)}\right] + \text{高阶误差项}
          $$
        - **策略性能改进条件**：$\mathbb{E}_{s \sim \nu^{\theta}} \mathbb{E}_{a \sim \theta'(\cdot|s)} [A^{\theta'}(s, a)] \geq 0$
      - **信任区域约束**：
        - 使用 KL 散度衡量策略差异：$D_{KL}(\theta || \theta') \leq \delta$
        - 在信任区域内进行线搜索，确保每次更新都满足约束且策略性能提升,<mark>约束新旧策略分布大约一致</mark>
        - 优化问题形式：
          $$
          \begin{aligned}
          &\max_{\theta'} \mathbb{E}_{s,a \sim \theta}\left[ \frac{\theta'(a|s)}{\theta(a|s)} A^{\theta'}(s,a) \right] \\
          &\text{s.t. } D_{KL}(\theta || \theta') \leq \delta
          \end{aligned}
          $$
      - **代理目标函数与二阶近似**：
        - **重要性采样比率**：$r_\theta(\theta') = \frac{\theta'(a|s)}{\theta(a|s)}$
        - **代理目标函数**（忽略高阶项）：
          $$
          L_{\theta}(\theta') = \mathbb{E}_{s,a \sim \theta}\left[ r_\theta(\theta') A^{\theta'}(s,a) \right]
          $$
        - **在 $\theta$ 处泰勒展开**（一阶近似）：
          $$
          L_{\theta}(\theta') \approx L_{\theta}(\theta) + g^T (\theta' - \theta)
          $$
          其中$g = \nabla_\theta L_{\theta}(\theta)$是策略梯度
        - **KL 散度的二阶近似**：
          $$
          D_{KL}(\theta || \theta') \approx \frac{1}{2}(\theta' - \theta)^T F (\theta' - \theta)
          $$
          其中$F$是 Fisher 信息矩阵：$F = \mathbb{E}_{s \sim \theta}\left[ \nabla_\theta \log \theta(a|s) \nabla_\theta \log \theta(a|s)^T \right]$
        - **近似优化问题**：
          $$
          \begin{aligned}
          &\max_{\theta'} g^T (\theta' - \theta) \\
          &\text{s.t. } \frac{1}{2}(\theta' - \theta)^T F (\theta' - \theta) \leq \delta
          \end{aligned}
          $$
      - **共轭梯度法求解**：
        - **拉格朗日乘子法**得到最优解：
          $$
          \theta'^* = \theta + \sqrt{\frac{2\delta}{g^T F^{-1} g}} F^{-1} g
          $$
        - **共轭梯度迭代**（求解$F^{-1}g$）：
          $$
          \begin{aligned}
          &x_0 = 0, \quad r_0 = p_0 = g \\
          &\alpha_k = \frac{r_k^T r_k}{p_k^T F p_k} \\
          &x_{k+1} = x_k + \alpha_k p_k \\
          &r_{k+1} = r_k - \alpha_k F p_k \\
          &\beta_k = \frac{r_{k+1}^T r_{k+1}}{r_k^T r_k} \\
          &p_{k+1} = r_{k+1} + \beta_k p_k
          \end{aligned}
          $$
          其中$F p_k$通过 Hessian-向量积计算，无需显式构造$F$
        - **Hessian-向量积**：
          $$
          F v = \nabla_\theta \left( \nabla_\theta D_{KL}(\theta || \theta')^T v \right)
          $$
      - **线搜索保证单调改进**：
        - **回溯线搜索**：从候选步长$\alpha = 1$开始，按$\alpha \leftarrow \alpha \times \beta$衰减（$\beta < 1$）
        - **接受条件**：
          $$
          \begin{aligned}
          &1. \quad L_{\theta}(\theta'_{new}) > L_{\theta}(\theta'_{old}) \\
          &2. \quad D_{KL}(\theta || \theta'_{new}) \leq \delta
          \end{aligned}
          $$
        - **参数更新**：$\theta' \leftarrow \theta'_{old} + \alpha \cdot \sqrt{\frac{2\delta}{g^T F^{-1} g}} F^{-1} g$
      - **GAE 优势估计**（广义优势估计）：
        - **TD 残差**：$\delta_t = r_t + \gamma V(s_{t+1}) - V(s_t)$
        - **GAE 公式**：
          $$
          A_t^{GAE(\gamma, \lambda)} = \sum_{l=0}^{\infty} (\gamma \lambda)^l \delta_{t+l}
          $$
          其中$\lambda \in [0, 1]$控制偏差 - 方差权衡
      - **算法流程**：
        1. 采样轨迹：使用当前策略 theta 采样轨迹数据
        2. 计算优势：使用 GAE 计算优势函数估计$A_t$
        3. 计算梯度：$g = \nabla_\theta L_{\theta}(\theta)$
        4. 共轭梯度：用共轭梯度法求解$F^{-1}g$
        5. 计算步长：$\Delta \theta = \sqrt{\frac{2\delta}{g^T F^{-1} g}} F^{-1} g$
        6. 线搜索：找到满足 KL 约束和目标提升的最大步长$\alpha$
        7. 更新策略：$\theta \leftarrow \theta + \alpha \Delta \theta$
        8. 更新价值函数：最小化$\mathbb{E}[(V(s) - V_{target})^2]$
      - **优势**：理论上保证策略单调改进，避免策略崩溃
      - **局限**：实现复杂，计算开销大，需要二阶优化方法

    - **PPO (Proximal Policy Optimization)** <mark>优化了 TRPO 中计算量复杂的问题</mark>
      - **核心机制**：
        - **核心创新：截断代理目标 (Clipped Surrogate Objective)**
          - 在更新策略时，限制新策略与旧策略的差异不能太大（通过 `clip` 操作）
          - 如果新策略比旧策略好太多，更新幅度会被截断；如果变差了，则不接受更新
        - **优势**：
          - **极其稳定**：避免了策略更新步长过大导致性能崩塌的问题
          - **样本效率高**：可以使用同一批数据进行多次梯度下降更新
          - **实现简单**：相比其前身 TRPO（需要复杂的二阶优化），PPO 只需一阶梯度
        
      - **PPO 的两种主要形式**：
        
        1. **PPO-惩罚 (PPO-Penalty)**
           - **基本思想**：使用拉格朗日乘数法直接将 KL 散度的限制放进目标函数中，将约束优化问题转化为无约束优化问题
           - **目标函数**：
             $$\arg\max_{\theta} \mathbb{E}_{s \sim \nu^{\pi_{\theta_k}}} \mathbb{E}_{a \sim \pi_{\theta_k}(\cdot|s)} \left[ \frac{\pi_{\theta}(a|s)}{\pi_{\theta_k}(a|s)} A^{\pi_{\theta_k}}(s, a) - \beta D_{KL}[\pi_{\theta_k}(\cdot|s), \pi_{\theta}(\cdot|s)] \right]$$
           - **自适应惩罚系数**：在迭代过程中不断更新 KL 散度前的系数 β
             - 令 $d_k = D_{KL}^{\nu^{\pi_{\theta_k}}}(\pi_{\theta_k}, \pi_{\theta})$，β 的更新规则：
               1. 如果 $d_k < \delta/1.5$，那么 $\beta_{k+1} = \beta_k/2$
               2. 如果 $d_k > \delta \times 1.5$，那么 $\beta_{k+1} = \beta_k \times 2$
               3. 否则 $\beta_{k+1} = \beta_k$
             - 其中 δ 是事先设定的超参数，用于限制学习策略和之前一轮策略的差距
           - **特点**：通过自适应调整惩罚系数来动态控制策略更新幅度
        
        2. **PPO-截断 (PPO-Clip)** ⭐ **更常用的形式**
           - **基本思想**：直接在目标函数中进行截断限制，保证新参数和旧参数的差距不会太大
           - **目标函数**：
             $$\arg\max_{\theta} \mathbb{E}_{s \sim \nu^{\pi_{\theta_k}}} \mathbb{E}_{a \sim \pi_{\theta_k}(\cdot|s)} \left[ \min\left( \frac{\pi_{\theta}(a|s)}{\pi_{\theta_k}(a|s)} A^{\pi_{\theta_k}}(s, a), \text{clip}\left(\frac{\pi_{\theta}(a|s)}{\pi_{\theta_k}(a|s)}, 1-\epsilon, 1+\epsilon\right) A^{\pi_{\theta_k}}(s, a) \right) \right]$$
           - **截断函数**：$\text{clip}(x, l, r) := \max(\min(x, r), l)$，即把 x 限制在 [l, r] 内
             - ε 是超参数，表示进行截断的范围（通常取 0.1 或 0.2）
           - **工作机制**：
             - **当优势函数 $A^{\pi_{\theta_k}}(s, a) > 0$ 时**（动作价值高于平均）：
               - 最大化目标函数会增大概率比 $\frac{\pi_{\theta}(a|s)}{\pi_{\theta_k}(a|s)}$
               - 但不会让其超过 $1+\epsilon$（防止更新过大）
             - **当优势函数 $A^{\pi_{\theta_k}}(s, a) < 0$ 时**（动作价值低于平均）：
               - 最大化目标函数会减小概率比 $\frac{\pi_{\theta}(a|s)}{\pi_{\theta_k}(a|s)}$
               - 但不会让其低于 $1-\epsilon$（防止更新过大）
           - **直观理解**：
             - 在梯度上升过程中，如果新策略优于旧策略，截断机制会限制提升幅度
             - 如果新策略差于旧策略，则完全不接受更新
             - 就像给策略更新安装了一个"安全带"，防止步子迈得太大
           - **优势**：
             - 无需复杂的二阶导数计算
             - 无需额外的超参数调优（相比 PPO-惩罚）
             - 在实践中表现更稳定，实现更简单
             - 支持多轮 epoch 训练，样本利用率更高

      - **DDPG (Deep Deterministic Policy Gradient)**
        - **核心思想**：结合 Actor-Critic 框架与 DQN 的思想，适用于连续动作空间的确定性策略梯度方法 
        - **网络结构**：
          - Actor 策略网络：输入状态，输出确定性动作（确定性策略）
          - Critic 价值网络：输入状态和动作，输出 Q 值
          - 各使用两个网络（更新网络/目标网络）分别来对 Actor-Critic 的策略和价值网络进行更新
        - **关键技术**：
          - **经验回放（Experience Replay）**：使用回放缓冲区存储转移样本，打破数据相关性
          - **目标网络（Target Network）**：Actor 和 Critic 都有对应的目标网络，提高训练稳定性
          - **软更新（Soft Update）**：目标网络参数通过滑动平均方式缓慢更新
            $$\theta' \leftarrow \tau \theta + (1 - \tau) \theta'$$
            其中 τ 是很小的常数（如 0.001），保证目标网络变化缓慢
          - **探索噪声**：在确定性策略输出的动作上添加高斯噪声或 OU 噪声，增加探索性
            $$a = \mu(s|\theta^\mu) + \mathcal{N}$$
        - **算法流程**：
          1. 初始化 Actor 和 Critic 网络及其目标网络
          2. Actor 根据当前状态输出确定性动作，并添加探索噪声
          3. 执行动作，观察奖励和下一状态，存储转移样本到回放缓冲区
          4. 从缓冲区采样批量数据
          5. Critic 更新：最小化 TD 误差
             $$L = \mathbb{E}[(Q(s,a|\theta^Q) - y)^2]$$
             $$y = r + \gamma Q'(s', \mu'(s'|\theta^{\mu'})|\theta^{Q'})$$
          6. Actor 更新：最大化期望 Q 值
             $$\nabla_{\theta^\mu} J \approx \mathbb{E}[\nabla_a Q(s,a|\theta^Q)|_{a=\mu(s)} \nabla_{\theta^\mu} \mu(s|\theta^\mu)]$$
          7. 软更新目标网络参数
        - **适用场景**：
          - 连续动作空间控制任务（如机械臂控制、机器人运动规划）
          - 需要精确控制动作幅度的场景
        - **优势**：
          - 能够处理高维连续动作空间
          - 结合了值函数方法和策略梯度方法的优势
          - 样本效率相对较高，训练稳定
        - **局限**：
          - 对超参数敏感，调参难度较大
          - 容易陷入局部最优
          - 在某些复杂环境中可能过估计 Q 值

      - **SAC (Soft Actor-Critic)**
        - **核心思想**：在策略函数中增加了熵的度量，来提高对高随机的策略的探索权重，并自动进行熵的权重调节，然后使用两个价值函数来压制价值过高估计的问题
        - **最大熵目标**：
          $$\max_\theta \mathbb{E}_{\pi_\theta}\left[\sum_{t=0}^T \gamma^t (r(s_t,a_t) + \alpha \mathcal{H}(\pi(\cdot|s_t)))\right]$$
          其中 α 是温度系数，控制熵正则化项的权重，𝓗是策略熵
        - **网络结构**：
          - Actor 策略网络：输入状态，输出动作的概率分布（随机策略）
          - 两个 Critic 价值网络：输入状态和动作，输出 Q 值（双 Q 网络抑制过估计）
          - 目标网络：Critic 的目标网络，软更新方式
        - **关键技术**：
          - **最大熵框架**：策略优化时同时最大化期望回报和策略熵
            $$J(\theta) = \mathbb{E}_{(s,a)\sim\rho_\beta}[Q(s,a) - \alpha \log \pi_\theta(a|s)]$$
          - **双 Q 网络**：使用两个独立的 Critic 网络，取最小值作为目标 Q 值，减少过估计
            $$y = r + \gamma \min_{i=1,2} Q_{\phi_i}(s',a') + \alpha \mathcal{H}(\pi)$$
          - **自动温度调节**：通过可学习的参数 α 自动调整熵正则化权重
            $$\alpha = \exp(\text{log\_alpha}), \quad \text{log\_alpha 可学习}$$
            - 当策略太确定（熵太低）时，自动增大 α ，强迫探索。
            - 当策略太随机（熵太高）导致性能下降时，自动减小 α ，偏向利用。
          - **重参数化技巧**：对连续动作采样使用重参数化，使梯度可以反向传播
            $$a = f_\theta(s; \epsilon), \quad \epsilon \sim \mathcal{N}(0,I)$$
          - **经验回放与软更新**：与 DDPG 类似，使用回放缓冲区和目标网络软更新
            $$\theta' \leftarrow \tau \theta + (1 - \tau) \theta'$$
        - **算法流程**：
          1. 初始化 Actor、双 Critic 网络及其目标网络，设置 log_alpha
          2. Actor 根据当前状态输出动作分布，采样得到动作并执行
          3. 存储转移样本 (s,a,r,s') 到回放缓冲区
          4. 从缓冲区采样批量数据
          5. Critic 更新：最小化 TD 误差，目标值包含熵项
             $$L_Q = \mathbb{E}[(Q(s,a) - y)^2]$$
             $$y = r + \gamma(1-d)(\min_{i} Q_{target,i}(s',a') + \alpha \mathcal{H}(\pi))$$
          6. Actor 更新：最大化期望 Q 值减去熵正则化项
             $$J_\pi = \mathbb{E}_{s\sim\mathcal{D},a\sim\pi}[\alpha \log \pi(a|s) - Q(s,a)]$$
          7. 自动调节 α：优化温度系数使策略熵接近目标值
             $$J_\alpha = \mathbb{E}_{a\sim\pi}[-\alpha \log \pi(a|s) - \alpha \mathcal{H}_{target}]$$
          8. 软更新 Critic 目标网络参数
        - **适用场景**：
          - 连续动作空间控制任务（如机器人控制、自动驾驶）
          - 需要高效探索和稳定训练的场景
          - 复杂环境中的精细操作任务
        - **优势**：
          - 最大熵框架提供自然的探索机制，无需额外添加噪声
          - 双 Q 网络有效抑制 Q 值过估计问题
          - 自动温度调节简化超参数调优
          - 样本效率高，训练稳定性好
          - 能够处理高维连续动作空间
          - 在保持性能的同时具有更强的鲁棒性
        - **局限**：
          - 需要维护多个网络，计算开销较大
          - 对目标熵的设定较为敏感
          - 在某些简单任务上可能过度探索

### 强化学习四大核心方法对比表

这是一个基于我们之前讨论的四种强化学习核心方法（**动态规划 DP**、**蒙特卡洛 MC**、**时序差分 TD**、**深度强化学习 Deep RL**）的详细对比表格。

这个表格涵盖了从理论基础到实际应用的各个维度，帮助你一目了然地看清它们的区别与联系。

| 维度 | **动态规划 (DP)** | **蒙特卡洛 (MC)** | **时序差分 (TD)** | **深度强化学习 (Deep RL)** |
| :--- | :--- | :--- | :--- | :--- |
| **核心定义** | 基于模型的规划方法 | 基于采样的无模型方法 | 结合采样与自举的无模型方法 | 使用神经网络近似价值/策略的 RL |
| **是否需要模型**($P, R$) | **需要** (必须已知完整模型) | **不需要** (Model-Free) | **不需要** (Model-Free) | **不需要** (通常 Model-Free) |
| **更新时机** | 同步/异步迭代 (无需交互) | **回合结束后** (必须完整轨迹) | **单步更新** (每走一步即可更新) | 单步或回合结束 (取决于具体算法) |
| **核心机制** | **自举 (Bootstrapping)**用估计值更新估计值 | **真实回报**用实际采样总和更新 | **自举 + 采样**用即时奖励+下一步估计更新 | **函数近似 (Function Approximation)**用神经网络拟合 Q 值或 $\pi$ |
| **偏差 (Bias)** | **有偏差** (依赖模型准确性) | **无偏差** (收敛于真值) | **有偏差** (依赖初始估计) | **有偏差** (依赖网络结构与初始化) |
| **方差 (Variance)** | **低** (确定性计算) | **高** (受整条轨迹随机性影响大) | **中/低** (仅受单步随机性影响) | **高** (受采样、网络初始化、超参影响) |
| **适用任务类型** | 任意 (回合制 / 持续任务) | **仅限回合制** (Episodic) | **任意** (回合制 / 持续任务) | **任意** (尤其擅长高维连续任务) |
| **状态空间处理** | **表格型** (显式存储每个状态) | **表格型** (显式存储访问过的状态) | **表格型** (显式存储访问过的状态) | **函数近似** (泛化到未见过的状态) |
| **维数灾难** | **严重** (状态多则无法计算) | **严重** (状态多则无法遍历) | **严重** (状态多则无法遍历) | **解决** (通过泛化处理高维状态) |
| **样本效率** | N/A (不需要采样，只需计算) | **低** (需大量完整回合) | **中高** (单步利用信息，收敛快) | **低** (通常需要海量交互数据) |
| **计算复杂度** | $O(|S|^2|A|)$ 每次迭代 | $O(1)$ 每次更新 (但需等待回合) | $O(1)$ 每次更新 | 高 (需进行反向传播梯度下降) |
| **典型算法** | 策略迭代 (Policy Iteration)价值迭代 (Value Iteration) | 蒙特卡洛控制First-visit / Every-visit MC | **Q-Learning** (Off-policy)**SARSA** (On-policy) | **DQN** (Value-based)**PPO**, A3C (Policy-based)**AlphaGo** (RL+Search) |
| **主要优点** | 理论完备，能找到全局最优解 | 简单直观，无模型，无偏差 | 在线学习，收敛快，方差较低 | 能处理图像/传感器等高维输入，泛化能力强 |
| **主要缺点** | 需要已知模型，无法应对大状态空间 | 只能用于回合制，方差大，收敛慢 | 有偏差，超参数敏感 | 训练不稳定，样本效率低，调参难 (“黑艺术”) |
| **应用场景** | 小规模网格世界已知规则的简单博弈 | 简单的回合制游戏模拟环境中的策略评估 | 机器人基础控制简单的导航任务 | **自动驾驶****复杂游戏 (Atari, Go, Dota2)****机器人精细操作****推荐系统** |

---

### 逻辑演进总结

为了更深刻地理解它们的关系，可以将它们看作是一个**不断放宽限制、增强能力**的演进过程：

1.  **DP (基石)**：
    *   **假设**：世界是完全已知的 ($P, R$)。
    *   **局限**：现实世界未知且状态太多。
    *   **贡献**：提出了**贝尔曼方程**和**自举**思想。

2.  **MC (突破模型限制)**：
    *   **改进**：不再需要知道 $P, R$，通过**采样**学习。
    *   **局限**：必须等到结局才能学习，方差大，不能处理持续任务。
    *   **贡献**：证明了无模型学习的可行性。

3.  **TD (突破时间与方差限制)**：
    *   **改进**：结合了 DP 的**自举**和 MC 的**采样**。不用等结局，单步即可更新，方差更低。
    *   **局限**：仍然是表格型方法，无法处理像图像这样的高维状态（维数灾难）。
    *   **贡献**：成为了现代 RL 的核心更新规则 (Q-Learning, SARSA)。

4.  **Deep RL (突破状态空间限制)**：
    *   **改进**：用**深度神经网络**替换了表格。解决了维数灾难，能处理像素、语音等高维输入。
    *   **代价**：引入了训练不稳定性、样本效率低等新问题。
    *   **贡献**：让强化学习真正走出了仿真小环境，进入了复杂的现实世界应用。

### 如何选择？

*   如果你有一个**小型的、规则完全已知**的问题（如简单的迷宫求解器）：选 **DP**。
*   如果你有一个**回合制**问题，且**不在乎方差**，想要最简单的实现：选 **MC**。
*   如果你需要一个**通用、高效、在线**的学习算法，且状态空间是**离散且较小**的：选 **TD (Q-Learning/SARSA)**。
*   如果你的输入是**图像、传感器数据**，或者状态空间**巨大/连续**：必须选 **Deep RL (DQN/PPO/SAC)**。


# 强化学习前沿算法

# 模仿学习 

## 问题背景

奖励函数并未给定，或奖励信号极其稀疏时效果不好

**核心思想：**
- 直接模仿最优策略在环境中交互的状态动作数据来训练策略**不需要用到环境提供的奖励信号**

## 主要方法

### 行为克隆 (Behavior Cloning, BC) <mark>拟合专家策略函数</mark>

**算法原理：**

BC 直接使用监督学习方法，将专家数据中的状态 $s$ 看作样本输入，动作 $a$ 视为标签，学习目标为：

$$\min_{\theta} \mathbb{E}_{(s,a) \sim \mathcal{D}_E}[L(f_\theta(s), a)]$$

其中：
- $\mathcal{D}_E$ 是专家的数据集
- $L$ 是对应监督学习框架下的损失函数
- 若动作是离散的，该损失函数可以是最大似然估计
- 若动作是连续的，该损失函数可以是均方误差函数

**特点分析：**
- ✅ 实现简单，可作为策略预训练方法
- ✅ 能快速学习到不错的策略（数据量充足时）
- ✅ 避免策略较差时低效探索，为强化学习创造高起点
- ❌ 存在**复合误差（compounding error）**问题
- ❌ 只能在专家数据的状态分布下预测准确
- ❌ 偏差累积：一旦遇到专家数据中未出现过的状态，策略可能随机选择动作，导致进一步偏离

**复合误差**

BC 学习到的策略在和环境交互过程中，只要存在一点偏差，就可能导致下一个遇到的状态是在专家数据中没有见过的。此时策略会随机选择动作，导致下一个状态进一步<mark>偏离专家策略的数据分布</mark>，最终在真实环境下效果不佳。

**疑问**

当问题足够复杂，数据量足够大时，会出现涌现现象吗？

---

### 生成式对抗模仿学习 (GAIL)

**算法原理：**

模仿专家策略的占用度量（occupancy measure），即尽量使得策略在环境中的所有状态动作对的占用度量和专家策略的占用度量一致，<mark>避免分布偏离</mark>

**占用度量的定义：**

占用度量 $\rho_\pi(s, a)$ 表示在策略 $\pi$ 下，智能体在长期运行中访问状态 $s$ 并执行动作 $a$ 的期望频率：
$$\rho_\pi(s, a) = \mathbb{E}_{\tau \sim \pi} \left[ \sum_{t=0}^\infty \gamma^t \mathbb{I}(s_t=s, a_t=a) \right]$$

GAIL 的优化目标等价于最小化策略占用度量 $\rho_\pi$ 与专家占用度量 $\rho_E$ 之间的 Jensen-Shannon 散度：
$$\min_\pi D_{JS}(\rho_\pi || \rho_E)$$

**核心架构：**

GAIL 包含两个组件：
1. **判别器（Discriminator）** $D_w(s, a)$
   - 输入：状态动作对 $(s, a)$
   - 输出：$0$ 到 $1$ 之间的实数，表示该状态动作对来自智能体策略而非专家的概率
   - 目标：区分专家数据和模仿者策略数据
   - <mark>本质作用：学习两个分布之间的密度比，而非简单记忆专家数据点</mark>

2. **策略（Policy）** $\pi_\theta$
   - 相当于生成器（Generator）
   - 目标：生成的轨迹能被判别器误认为专家轨迹
   - <mark>通过环境交互不断探索新状态，被迫在所有可达状态上模仿专家行为分布</mark>

**优化目标：**

判别器的损失函数：
$$\max_w \mathbb{E}_{(s,a) \sim \pi_E}[\log D_w(s,a)] + \mathbb{E}_{(s,a) \sim \pi_\theta}[\log(1 - D_w(s,a))]$$

策略的训练方式：
- 用判别器的输出作为奖励信号：$r(s,a) = \log D_w(s,a)$
- 使用任意强化学习算法（如 PPO）训练策略
- <mark>通常包含最大熵正则化项，防止过拟合专家轨迹的具体路径</mark>

**训练流程：**
1. 策略与环境交互，采样新的状态动作对
2. 基于专家数据和策略新采样的数据训练判别器
3. 将判别器输出转换为策略的奖励信号
4. 使用强化学习算法更新策略
5. 重复上述过程，直到策略生成的数据分布接近专家数据分布

**为什么静态专家数据能拟合动态分布？**

这是 GAIL 最深刻的理论洞见。关键在于以下三个机制的协同作用：

1. **判别器学习的是分布差异而非数据点匹配**
   - 判别器是函数近似器（神经网络），学习的是分类边界而非查表匹配
   - 即使专家数据有限，判别器会在整个状态空间形成连续的"奖励地形图"
   - 策略进入专家未覆盖区域时，判别器给出低分，迫使策略调整行为

2. **环境动力学的传播效应**
   - 环境转移概率 $P(s'|s,a)$ 会将局部偏差传播到新状态
   - 策略在某状态的微小偏离会被环境带到新状态，在那里继续接受判别器评估
   - 通过充分探索，策略被迫在**所有可达状态**上与专家占用度量对齐

3. **数学收敛性保证**
   - GAIL 的对抗博弈被证明收敛于 $\min_\pi D_{JS}(\rho_\pi || \rho_E)$
   - 只要判别器足够强大且策略采样充分，优化过程强制 $\rho_\pi \to \rho_E$
   - 不需要专家再次采样，因为**环境动态性 + 判别器分布鉴别能力**足以引导策略复现专家占用度量

**类比理解：**

学习画画的过程：
- **专家数据**：几幅名画（静态）
- **判别器**：艺术评论家，能识别"大师神韵"而非照抄像素
- **策略采样**：不断创作新画作
- **结果**：为骗过评论家，必须理解大师的笔触和构图逻辑（占用度量），而非背诵具体画作

**与 BC 的区别：**
- GAIL 需要和环境交互收集数据 <mark>克隆专家策略的占用度量</mark>
- BC 完全不需要和环境交互 <mark>只是克隆专家策略</mark>
- GAIL 避免了 BC 的复合误差问题（covariate shift）

**特点分析：**
- ✅ 训练目标贴合模仿学习任务（直接拉近占用度量）
- ✅ 避免了 BC 的复合误差问题
- ✅ 策略可以和环境交互出更多数据，生成"量身定做"的指导奖励信号
- ✅ 在少量专家数据下仍能取得很好效果
- ✅ 具有理论保证的全局分布匹配性质
- ❌ 计算复杂度较高，需要同时训练判别器和策略
- ❌ 需要大量环境交互
- ❌ 对判别器容量和采样充分性有较高要求

---

### 3. 逆强化学习 (Inverse Reinforcement Learning, IRL)

**基本原理：**
- 假设环境的奖励函数应该使得专家轨迹获得最高的奖励值
- 学习专家行为背后的奖励函数
- 基于学习到的奖励函数进行正向强化学习，得到模仿策略

**核心思想：**
- 在奖励函数未给定或奖励信号极其稀疏的场景下，通过观察专家示范来推断奖励函数
- 专家认为重要的状态和动作应该获得更高的奖励
- 将专家轨迹与智能体策略生成的轨迹进行对比，调整奖励函数使专家轨迹的期望回报更高

**数学形式化：**
- 给定专家演示轨迹集 $\mathcal{D}_E = \{\tau_1, \tau_2, ..., \tau_m\}$
- 寻找奖励函数 $R^*$ 使得专家策略的期望回报最大化：
  $$R^* = \arg\max_{R} \mathbb{E}_{\tau \sim \pi_E}[R(\tau)] - \mathbb{E}_{\tau \sim \pi}[R(\tau)]$$
- 其中 $\pi_E$ 是专家策略，$\pi$ 是当前学习到的策略

**主要方法：**

1. **基于线性规划的学徒学习 (Apprenticeship Learning)**
   - 假设奖励函数可以表示为状态特征的线性组合：$R(s) = w^T \phi(s)$
   - 通过线性规划找到权重 $w$，使专家策略的表现优于任何其他策略
   - 代表算法：Abbeel & Ng (2004) 提出的学徒学习算法

2. **最大熵逆强化学习 (Maximum Entropy IRL)**
   - 引入最大熵原理来解决奖励函数的歧义性问题
   - 假设专家以概率分布选择轨迹，高回报轨迹被选中的概率更大
   - 使用梯度下降优化奖励函数参数

3. **引导代价学习 (Guided Cost Learning)**
   - 结合深度学习和采样方法学习复杂的非线性奖励函数
   - 适用于高维连续状态空间的任务

**算法流程：**
```
输入：专家轨迹 D_E，初始策略 π_0
输出：最优策略 π*

1. 初始化奖励函数 R_0
2. for i = 0, 1, 2, ... do:
   a. 基于当前奖励函数 R_i，使用 RL 算法学习策略 π_i
   b. 用策略 π_i 采样轨迹 D_i
   c. 比较 D_E 和 D_i 的特征期望
   d. 更新奖励函数 R_{i+1}，增加专家轨迹的相对优势
   e. if 策略收敛 then return π_i
```

**特点：**
- ✅ 能够理解专家行为背后的"意图"和"目标"
- ✅ 学到的奖励函数可以泛化到新的场景
- ✅ 有良好的学术贡献，理论体系完善
- ❌ 计算复杂度高，需要反复求解正向强化学习问题
- ❌ 存在奖励函数的歧义性问题（多个奖励函数都能解释专家行为）
- ❌ 实际应用价值较小，主要用于理论研究

**应用场景：**
- 机器人路径规划：通过观察人类操作学习机器人的运动目标
- 自动驾驶：从人类驾驶数据中学习驾驶偏好和安全准则
- 博弈论：分析对手的策略目标和支付函数

**GAIL 与传统 IRL 的对比：**

虽然 GAIL 在大多数高维、复杂的连续控制任务（如机器人行走、自动驾驶模拟）中表现优于传统 IRL 算法，但它并非在所有方面都完全更好。两者各有优劣：

**GAIL 的优势：**
- **计算效率高**：将奖励推断和策略学习合并为端到端的对抗过程，无需传统 IRL 的内循环（每更新一次奖励函数都要重新跑一遍完整 RL），训练速度快得多
- **适合高维状态空间**：利用深度神经网络作为判别器，能够处理图像输入或高维传感器数据，无需人工设计特征
- **自动特征学习**：判别器自动从原始数据中学习特征表示，避免了传统 IRL 需要专家手动定义特征函数的繁琐过程

**传统 IRL 的优势：**
- **奖励函数可解释性高**：输出显式的数学公式（如 R(s,a) = -0.5×速度² + 10×安全距离），人类可读，适合安全关键领域（医疗、航空）
- **跨环境迁移能力强**：学到的奖励函数可应用到不同动力学环境，例如从仿真车迁移到真车，只需重新训练策略即可
- **理论保证更强**：在小样本、低维离散状态下，基于最大边际的 IRL 算法有更强的收敛保证
- **训练更稳定**：不存在 GAN 固有的模式坍塌、超参数敏感等问题

**GAIL 的缺陷：**
- 训练不稳定，容易模式坍塌（只学会专家的一种行为）
- 超参数敏感，调参难度高
- 缺乏明确的停止准则
- 策略过拟合训练环境，环境变化后需重新训练

# 模型预测控制

## 核心思想

模型预测控制（Model Predictive Control，MPC）是一种**基于模型**（model-based）的强化学习方法。与 DQN、REINFORCE、Actor-Critic 等无模型（model-free）方法不同，MPC 通过建立环境模型来帮助智能体训练和决策。

MPC 的关键特点：**不构建显式策略**，而是根据环境模型来选择当前要采取的动作。

## 工作原理

MPC 采用迭代的两阶段过程：

1. **学习环境模型**：根据历史数据拟合环境动态
2. **模型预测控制**：在与真实环境交互时，使用环境模型选择动作

### 决策流程

在每一步决策时：
- 生成多个候选动作序列
- 用环境模型推演每个序列的未来结果
- 选择累积奖励最大的序列
- **执行最优序列的第一个动作**
- 下一步重复上述过程

目标函数：最大化推演长度内的累积奖励

```
A* = argmax_A Σᵢ₌₀ᴴ γⁱ r(sₜ₊ᵢ, aₜ₊ᵢ)
aₜ = A*[0]  # 取最优序列的第一个动作
```

## 打靶法（Shooting Methods）

如何生成候选动作序列是 MPC 的核心问题，称为打靶（shooting）。

### 随机打靶法

最简单的方法：随机采样生成动作序列
- 从动作空间随机采样动作
- 组合成条长度为 H 的序列
- 适用于简单环境，效果尚可

### 交叉熵方法（CEM）

更高效的进化策略方法，核心思想：维护一个带参数的分布，根据采样结果更新分布参数。

**算法流程**：
```
for 迭代次数 do
    从分布 μ 中采样 N 条动作序列
    用环境模型评估每条序列的累积奖励
    保留前 M 条最优序列（精英样本）
    用精英样本更新分布参数 μ
end for
取所有最优序列第一个动作的均值作为当前动作
```

对于连续动作空间，通常使用**截断正态分布** N(μ, σ²)，其中 μ 为均值向量，σ 为标准差。

## PETS 算法

PETS（Probabilistic Ensembles with Trajectory Sampling）：结合概率集成与轨迹采样的 MPC 算法。

### 环境模型构建

#### 不确定性建模

系统中有两种不确定性：
- **偶然不确定性**（aleatoric uncertainty）：系统本身的随机性
- **认知不确定性**（epistemic uncertainty）：数据不足导致的认知局限

#### 概率动力学模型

将环境模型输出定义为高斯分布，捕捉偶然不确定性：

```
p(s'|s,a) = N(μ(s,a), Σ(s,a))
```

使用神经网络学习均值函数 μ 和协方差矩阵 Σ。

损失函数（负对数似然）：
```
L(θ) = Σᵢ ||μ(sᵢ,aᵢ) - s'ᵢ||² / (2σ²) + log σ
```

#### 集成学习

构建 K 个结构相同但参数不同的神经网络：
- 不同的随机初始化
- 每次训练使用不同的数据子集（bootstrap 采样）
- 捕捉认知不确定性

### 轨迹采样

使用集成模型进行多步预测：
- 每条轨迹采样时，每一步随机选择一个模型
- 一条轨迹会经过多个不同的环境模型
- 增加预测的鲁棒性

### CEM 优化

在 PETS 中，CEM 用于在模型集成上进行轨迹优化：
- 采样大量动作序列
- 用集成模型预测每条轨迹的奖励
- 选择精英样本更新动作分布
- 返回最优动作

### 算法实践要点

#### 模型架构设计

- 每一层都设计为集成形式
- 使用高斯分布输出（均值 + 方差）
- 早停策略：验证集 5 次无提升则停止训练

#### 经验回放池

除存储转移样本外，还需支持返回全部数据，用于模型训练。

### 性能分析

#### 优势

- **样本效率高**：比 SAC 等无模型方法少得多的交互次数即可达到相近效果
- **无需显式策略**：避免策略训练的复杂性
- **长程规划能力**：可进行多步推演和优化

#### 局限性

- **计算复杂度高**：每次动作选择需大量模拟，速度慢
- **模型误差累积**：多步推演后准确性显著下降
- **实时性差**：不适用于对及时性要求高的系统
- **复杂系统受限**：简单控制策略可能不足以应对高度复杂的系统


## MBPO 算法 

基于短序列模拟数据增强无模型算法的策略训练

### 算法简介

基于模型的策略优化 (Model-Based Policy Optimization，MBPO) 算法是加州大学伯克利分校的研究员在 2019 年的 NeurIPS 会议中提出的。MBPO 成为深度强化学习中最重要的基于模型的强化学习算法之一。

MBPO 算法结合了 Dyna-Q 算法和 PETS 算法的思想：
- 与 Dyna-Q 类似，利用模型生成虚拟数据辅助策略学习
- 与 PETS 类似，学习用神经网络表示的环境模型，适用于连续型状态和动作空间
- 采用 SAC（Soft Actor-Critic）作为无模型强化学习部分

相比无模型的强化学习方法，MBPO 通过和模型进行交互产生额外的虚拟数据，对真实环境中样本的需求量减少，因此通常具有更高的采样效率。

### 核心观察

MBPO 算法基于以下两个关键的观察：

**观察 1：复合误差累积**
随着环境模型的推演步数变长，模型累积的复合误差会快速增加，使得环境模型得出的结果变得很不可靠。

**观察 2：权衡关系**
必须权衡推演步数增加后模型增加的误差带来的负面作用与步数增加后使得训练的策略更优的正面作用，二者的权衡决定了推演的步数。

### 分支推演

基于上述观察，MBPO 提出了分支推演（branched rollout）的概念：

只使用模型来从之前访问过的真实状态开始进行较短步数的推演，而非从初始状态开始进行完整的推演。即在原来真实环境中采样的轨迹上面推演出新的"短分支"。

这样做的好处是：
- 使模型的累积误差不至于过大
- 保证最后的采样效率和策略表现
- 分支推演的长度是平衡样本效率和策略性能的重要超参数

### 算法流程

MBPO 算法框架如下：

1. **初始化**：策略、环境模型参数、真实环境数据集、模型数据集

2. **循环执行以下步骤**：
   - 通过环境数据来训练模型参数
   - 在每个时间步：
     - 根据策略与环境交互，并将交互的轨迹添加到真实数据集
     - 进行模型推演：
       - 从真实数据集中均匀随机采样一个状态
       - 以该状态为初始状态，在模型中使用策略进行 k 步的推演
       - 将生成的轨迹添加到模型数据集
     - 基于模型数据，使用 SAC 来更新策略参数

### 理论分析

#### 性能提升的单调性保障

基于模型的方法希望保证在模型环境中提高策略性能时，在真实环境中也能取得策略性能的提升。具体可形式化为：

$$\eta(\pi) \geq \eta_{\text{model}}(\pi) - \epsilon$$

其中，$\eta(\pi)$ 表示策略在真实环境中的期望回报，$\eta_{\text{model}}(\pi)$ 表示策略在模型环境中的期望回报。

在 MBPO 中，根据泛化误差和分布偏移估计出这样一个下界：

$$\eta(\pi) \geq \eta_{\text{model}}(\pi) - c \cdot \epsilon_m - d \cdot D_{\text{shift}}$$

其中，$\epsilon_m$ 刻画了模型泛化误差，$D_{\text{shift}}$ 刻画了当前策略与数据收集策略之间的策略转移（policy shift）。

#### 最优推演步长

如果使用当前策略来估计模型误差（记为 $\epsilon_m$），那么有：

$$\eta_{\text{branch}}(\pi) \geq \eta_{\text{model}}(\pi) - k \cdot \epsilon_m$$

并且对其进行线性近似：

$$\eta_{\text{branch}}(\pi) \approx \eta_{\text{model}}(\pi) - k \cdot \epsilon_m$$

结合分支推演，可以得到一个新的策略期望回报界：

$$\eta(\pi) \geq \eta_{\text{branch}}(\pi) - \text{error terms}$$

通过以上公式，可以得到理论最优的推演步长：

$$k^* = \arg\max_k [\text{performance gain}(k) - \text{error growth}(k)]$$

对于推演步数 k，当 k 变大时，性能增益变小，而误差增长变大。如果模型误差足够小的话，最优推演步长就是正数，此时分支推演就是一个有效的方法。

在主流的机器人运动环境 MuJoCo 的典型场景中，模型误差的数量级非常小，大约都在小区间，而且它随着训练数据的增多而不断下降，说明模型的泛化能力逐渐增强。但在高随机性的离散状态环境中，往往环境模型的拟合精度较低，此时使用基于分支推演的方法效果有限。

### 算法特点

**优点**：
- **高样本效率**：相比无模型方法，大幅减少对真实环境样本的需求
- **适用范围广**：在复杂环境（如 Hopper、HalfCheetah）中表现优异
- **理论基础扎实**：提供了分支推演步数与模型误差、策略偏移之间的定量关系
- **误差可控**：通过短分支推演避免累积误差过大

**缺点**：
- **依赖模型精度**：在模型难以拟合的环境中效果受限
- **超参数敏感**：分支推演长度需要仔细调节
- **不适用于高随机环境**：在高随机性离散状态环境中应用有限

## 离线强化学习

### 核心概念

**定义**：离线强化学习（offline reinforcement learning）的目标是在智能体不和环境交互的情况下，仅从已经收集好的确定数据集中，通过强化学习算法得到比较好的策略。

**产生背景**：传统强化学习算法（无论在线策略还是离线策略）都需要智能体在训练过程中不断与环境交互获取新反馈数据。然而现实场景中，让尚未学习好的智能体与环境交互可能导致危险或巨大损失：
- **自动驾驶**：训练初期策略不成熟可能导致事故
- **推荐系统**：早期不佳的用户体验会导致用户流失
- **医疗决策**：错误的探索可能危及患者安全

**与在线学习的区别**：
- 在线强化学习：智能体可通过环境交互及时修正误差
- 离线强化学习：智能体无法交互，需限制外推误差影响

### 关键技术

#### 外推误差问题

**定义**：外推误差（extrapolation error）是指由于当前策略可访问的状态动作对与数据集采样得到的状态动作对分布不匹配而产生的误差。

**实验验证**：研究者通过三个实验发现离线 DDPG 智能体表现远差于在线版本：
1. **完全回放**：记录在线智能体所有交互数据用于离线训练
2. **同步训练**：使用相同数据顺序同步训练在线和离线智能体
3. **模仿训练**：使用优秀专家数据训练离线智能体

结果：三个实验中离线智能体表现均不佳，即使面对优质数据也未能有效学习。

#### 批量限制 Q-learning（BCQ）

**基本思想**：<mark>只访问与数据集中相似的数据</mark>，实现批量限制策略（batch-constrained policy）。

**三个目标**：
- 最小化选择动作与数据集的距离
- 到达与离线数据集状态相似的状态
- 最大化价值函数

**技术实现**：
- **生成模型**：训练 VAE 生成接近数据集的动作
- **扰动模型**：引入微小扰动增加动作多样性
- **策略形式**：

$$\pi(a|s) = \text{perturb}(G(s), s)$$

其中 G 为生成模型，perturb 为扰动模型。

**算法流程**：
1. 初始化价值网络、扰动网络、生成网络及其目标网络
2. 从数据集采样状态转移
3. 编码器生成动作的均值和标准差
4. 解码器生成候选动作
5. 对动作施加扰动后计算目标值
6. 更新各网络参数
7. 软更新目标网络

**理论保证**：若数据包含所有可能的状态动作对，算法可收敛到最优价值函数。

#### 保守 Q-learning（CQL）

**基本思想**：

- 通过对远离数据集的点上的价值函数保持低值来减少外推误差。
- <mark>压低数据集中不存在</mark>的动作的 Q 值

**核心机制**：在贝尔曼方程中引入额外限制项，惩罚高估的价值估计。

**迭代方程**：

$$Q_{k+1}(s,a) = \mathcal{B}^*Q_k(s,a) - \alpha \left( \frac{Q(s,a)}{\sum_a \exp(Q(s,a))} \right)$$

其中 α 为平衡因子，第二项为保守惩罚项。

**简化形式**：通过数学推导可化简为不含行为策略的形式：

$$Q_{k+1}(s,a) = \mathcal{B}^*Q_k(s,a) - \alpha \log \frac{\sum_a \exp(Q_k(s,a))}{|A|}$$

**算法流程**（基于 SAC 框架）：
1. 初始化价值网络、目标网络和策略网络
2. 更新熵正则系数
3. 更新价值函数（含保守惩罚项）
4. 更新策略网络
5. 重复直至收敛

**优势**：
- 直接限制价值函数，对策略无特殊要求
- 计算效率高，无需完整离线策略评估
- 适用于 DQN 和 SAC 等多种框架

### 评估方法

**数据集质量影响**：
- 混合数据集（包含不同阶段策略采样）通常优于单一数据集
- 数据集覆盖度越高，算法表现越稳定
- 需要高质量专家数据才能保证最终策略性能

**调参挑战**：
- 对超参数极为敏感
- 无法像模拟器中那样实时评估策略
- 需要谨慎选择停止训练的时机

### 应用边界

**适用场景**：
- 高风险环境（自动驾驶、医疗）
- 数据收集成本高昂的场景
- 已有大量历史数据的领域
- 需要利用旧策略数据改进的场景

**不适用场景**：
- 环境动态变化剧烈的场景
- 缺乏足够历史数据的新兴领域
- 需要持续在线适应的任务

### 实践要点

**数据收集策略**：
- 使用多个不同策略采集数据以增加覆盖度
- 平衡探索与利用，避免数据过于集中在局部
- 考虑使用先验知识引导数据采集

**实现技巧**：
- BCQ 需要使用 VAE 等生成模型，实现复杂度较高
- CQL 实现相对简单，在 SAC 基础上修改即可
- 正则项通常取均匀分布作为先验策略

### 风险与挑战

**主要挑战**：
- **分布偏移**：测试时策略分布与训练数据分布不匹配
- **过拟合风险**：可能在有限数据集上过拟合
- **评估困难**：缺乏可靠的离线评估方法
- **保守性过度**：可能过于保守而无法充分利用数据

**现实问题**：
- 离散策略评估和选择困难
- 数据收集策略的保守性设计
- 数据缺失或不完整的处理
- 如何确定最优停止时机

### 与其他方法对比

**与模仿学习的关系**：
- 共同点：都利用已有数据，避免在线探索
- 差异点：模仿学习通常只能达到专家水平，离线 RL 可超越专家

**与基于模型方法的结合**：

基于模型的离线强化学习方法采用悲观的马尔可夫决策过程框架，核心思想是“对动力学悲观”。这类方法通过学习环境动力学模型生成虚拟数据，同时利用不确定性量化来防止模型偏差被策略利用。

**MOReL（Model-Based Offline Reinforcement Learning）**：

- **核心机制**：采用硬性截断策略，构建悲观 MDP
- **不确定性度量**：训练多个动力学模型组成集成，使用模型集合内的分歧作为不确定性度量
- **惩罚方式**：当状态动作对的模型预测分歧超过阈值时，直接终止轨迹并给予极大负惩罚（HALT）
- **工作原理**：在数据覆盖良好的区域，多个模型预测结果一致（方差小），允许策略探索；在分布外区域，模型预测分歧大（方差大），触发终止机制
- **优势**：实现简单，理论保证强，能有效防止策略利用模型漏洞
- **局限**：硬截断可能导致过于保守，限制策略的泛化能力

**MOPO（Model-Based Offline Policy Optimization）**：

- **核心机制**：采用软性惩罚，修改奖励函数
- **不确定性度量**：学习输出状态条件高斯分布的动力学模型集合，使用预测方差的最大值度量不确定性
- **惩罚方式**：在模型生成的虚拟奖励上减去不确定性项，形成保守奖励函数

$$\tilde{r}(s,a) = \hat{r}(s,a) - \lambda \cdot \max_{i} ||\Sigma_i(s,a)||_F$$

其中$\hat{r}$为模型预测奖励，$\lambda$为惩罚系数，$\Sigma$为模型预测的不确定性（方差矩阵的 Frobenius 范数）

- **工作原理**：允许智能体在虚拟世界中自由探索，但在模型不确定的区域，奖励会被大幅扣减甚至变为负数，迫使策略在模型“确信”的范围内泛化
- **理论保证**：最大化真实 MDP 策略回报的下界
- **优势**：相比硬截断更灵活，能在安全和探索之间取得平衡
- **特点**：无需显式知道行为策略，适用于多任务迁移

## HER 算法

**HER（Hindsight Experience Replay，事后经验回放）** 是 2017 年 NeurIPS 提出的目标导向强化学习（Goal-Oriented Reinforcement Learning, GoRL）经典算法，用于解决稀疏奖励问题。

传统的强化学习主要关注如何最大化长期的累积奖励（Cumulative Reward）。而 GORL 则专注于让智能体（Agent）学习如何达成特定的、明确的目标（Goal）

### 核心思想

通过重新定义历史轨迹的目标，选择其中成功的部分，达到数据增强的目的

### 问题定义

目标导向的强化学习使用扩展的 MDP 定义：

其中：
- ：状态空间
- ：动作空间
- ：状态转移函数
- ：目标空间
- ：状态到目标的映射函数
- ：与目标相关的奖励函数

奖励函数常见形式：

优化目标：最大化

### HER 算法流程

1. 初始化策略参数 和经验回放池 
2. For 每个序列：
   - 根据环境给定目标 和初始状态 ，使用策略 采样得到轨迹 
   - 将轨迹以 形式存入 
   - 从 中采样 个元组 
   - 对每个元组，选择状态 ，映射为新目标 并计算新奖励
   - 用新数据替换原元组，训练策略
3. End for

### 目标选择策略

HER 提出 3 种状态 选择方案：

- **future**：选择同一轨迹中时间上在后的状态作为 （效果最好）
- **episode**：选择同一轨迹中的任意状态作为 
- **random**：选择经验回放池中的随机状态作为 

### 优势

- 解决稀疏奖励导致的训练缓慢问题
- 从失败经验中提取有用信息
- 提升训练稳定性和样本效率
- 可结合任意策略优化算法（DQN、DDPG 等）

## 多智能体强化学习

### 多智能体强化学习简介

多智能体强化学习（Multi-Agent Reinforcement Learning, MARL）研究多个智能体在环境中交互和学习的问题。相比于单智能体，MARL 的难点主要体现在：

- **环境非稳态性**：每个智能体视角下，环境是<mark>非稳态的</mark>（non-stationary），同一状态下采取相同的动作，所得到的状态转移和奖励函数分布可能在不断改变，因为其他智能体也在学习和改变策略
- **多目标优化**：不同智能体需要最大化自己的利益，训练目标可能是多目标的
- **训练复杂度增加**：可能需要大规模分布式训练来提高效率

### 问题建模

多智能体环境用一个元组表示：，其中：

- ：智能体的数目
- ：所有智能体的状态集合
- ：所有智能体的动作集合
- ：所有智能体奖励函数的集合
- ：环境的状态转移概率

目标是为每个智能体学习一个策略来最大化其自身的累积奖励。

### 多智能体强化学习的基本求解范式

#### 1. 完全中心化（Fully Centralized）方法

将所有智能体的决策当作一个超级智能体：

- **优点**：环境仍然是稳态的，单智能体算法的收敛性可以得到保证
- **缺点**：维度爆炸，训练复杂度巨幅提升，难以扩展到智能体数量多或环境大的情况

#### 2. 完全去中心化（Fully Decentralized）方法

每个智能体独立学习，不考虑其他智能体的改变：

- **优点**：扩展性好，不会遇到维度灾难
- **缺点**：环境非稳态，训练的收敛性不能得到保证

### IPPO 算法（Independent PPO）

IPPO 是一种完全去中心化的算法，属于独立学习（independent learning）方法。对每个智能体使用单智能体 PPO 算法进行训练。

**算法流程：**

1. 对于个智能体，为每个智能体初始化各自的策略以及价值函数
2. for 训练轮数 do:
   - 所有智能体在环境中交互分别获得各自的一条轨迹数据
   - 对每个智能体，基于当前的价值函数用 GAE 计算优势函数的估计
   - 对每个智能体，通过最大化其 PPO-截断的目标来更新其策略
   - 对每个智能体，通过均方误差损失函数优化其价值函数
3. end for

**参数共享技巧：**

对于同质的智能体（状态空间和动作空间完全一致，优化目标也一致），可以使用同一套策略参数，这样可以：

- 使模型训练数据更多
- 提高训练稳定性

**特点：**

- 当智能体数量较少时能取得一定效果
- 最终胜率有限，智能体间无法有效合作
- 智能体数量增加时（如到 5 个），训练效果不佳

### CTDE 范式：中心化训练去中心化执行

**CTDE（Centralized Training with Decentralized Execution）** 是一种进阶的多智能体强化学习范式。

**核心思想：**

- **训练时**：使用一些单个智能体看不到的全局信息，以达到更好的训练效果
- **执行时**：不使用这些信息，每个智能体完全根据自己的策略直接动作，达到去中心化执行的效果

**优势：**

- 训练时能有效利用全局信息，达到更好且更稳定的训练效果
- 推断时可以仅利用局部信息，具有一定的扩展性

**类比理解：**
足球队的训练和比赛过程：

- 训练时：11 个球员可以直接获得教练的指导（掌握比赛全局信息）完成整体配合
- 比赛时：训练好的球员根据场上实时情况直接做出决策，不再有教练指导

**CTDE 算法分类：**

1. **基于值函数的方法**：如 VDN、QMIX 等
2. **基于 Actor-Critic 的方法**：如 MADDPG、COMA 等

### MADDPG算法（Multi-Agent DDPG）

MADDPG 是 CTDE 范式中基于 Actor-Critic 的经典算法。

**基本思想：**

- 每个智能体用一个 DDPG算法训练
- 所有智能体共享一个中心化的 Critic 网络
- Critic 网络在训练过程中同时对每个智能体的 Actor 网络给出指导
- 执行时每个智能体的 Actor 网络完全独立做出行动（去中心化执行）

#### 数学形式化

考虑一个有个智能体的博弈：

- ：代表个智能体所有可能的状态空间（全局信息）
- ：每个智能体的动作空间
- ：每个智能体的观测空间
- ：每个智能体的策略（概率分布）
- ：环境的状态转移函数
- ：每个智能体的奖励函数
- ：每个智能体从全局状态得到的部分观测信息
- ：初始状态分布

**随机性策略的策略梯度：**

其中，是一个中心化的动作价值函数，包含了所有智能体的观测和动作。

**确定性策略的策略梯度：**

其中，是经验回放池，存储的数据为 。

**Critic 网络的损失函数更新：**

其中，是更新价值函数中使用的目标策略的集合（延迟更新的参数）。

#### MADDPG算法流程

1. 随机初始化每个智能体的 Actor 网络和 Critic 网络
2. for 序列 do:
   - 初始化一个随机过程，用于动作探索
   - 获取所有智能体的初始观测
   - for do:
     - 对于每个智能体 ，用当前的策略选择一个动作
     - 执行动作 并且获得奖励和新的观测
     - 把 存储到经验回放池中
     - 从中随机采样一些数据
     - 对于每个智能体，中心化训练 Critic 网络
     - 对于每个智能体，训练自身的 Actor 网络
     - 对每个智能体，更新目标 Actor 网络和目标 Critic 网络
   - end for
3. end for

#### 离散动作空间的处理：Gumbel-Softmax 技巧

MADDPG 要求动作对策略参数可导，但离散动作空间不满足这个条件。可以使用 **Gumbel-Softmax** 方法来得到离散分布的近似采样。

**重参数化方法：**

假设有一个随机变量服从离散分布，其中表示 且满足 。引入重参数因子 （采样自的噪声）：

**Gumbel-Softmax 采样：**

通过计算离散值，该离散值近似等价于离散采样的值，并且自然地引入了对于的梯度。

**温度参数的作用：**

- 越小，生成的分布越趋向于的结果
- 越大，生成的分布越趋向于均匀分布
